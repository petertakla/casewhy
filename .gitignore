# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env
.env*.local
.env

# misc
.DS_Store
*.pem
npm-debug.log*

# Claude Code session state
.claude/

# TypeScript incremental build cache
*.tsbuildinfo
.vercel
.env*

# Round 110 — generated by prebuild (scripts/build-search-index.ts) on
# every build; committing it would go stale the moment content changes
# without a redeploy, and creates a diff on every unrelated commit.
/public/search-index.*.json
