# CaseWhy — Project Context for Claude Code

This file is the standing handoff between the cloud Claude session (which owns business/planning work and the artifacts below) and Claude Code sessions running here on the Fedora machine (which own the codebase). Read this first; update it — and tell the user to mention updates back to the cloud session — whenever a status below changes.

> **Last updated by Claude Code: Sep 5, 2026, ~4:15am ET.** If you (cloud session) are reading a copy of this file fetched before that time, re-fetch it now before editing — an earlier stale-copy edit reverted two already-resolved sections back to "unresolved" once already tonight (see the process note under "Right now" below). **Every item on the "Next build priorities" list is now fully done, verified live, real email included** — explanation layer, persistent tracking + magic-link auth, encryption at rest, file upload stub, lint fix, and email notifications (code + cron-job.org scheduling + Postmark, all live, a real test email confirmed sent). All live on `casewhy-app.vercel.app`, CI green (typecheck + lint + build), everything pushed to `nextjs-app`. `tracked_cases` is empty (clean slate — test data cleaned up along the way). One caveat: Postmark is still in **test mode** (100-email cap, can only send to `casewhy.com` addresses) — real users need "Request approval" submitted and granted first, a user-only step not yet done. Also: the user wants all CaseWhy email, sent or received, to use `info@casewhy.com` — not their personal address. Relatedly: Claude Code's Gmail tool access here is tied to the user's personal Gmail, same as the cloud session's own connector — recommended fix is Gmail delegation (add the personal address as a delegate on `info@casewhy.com`, Gmail settings → Accounts) rather than a second connector, since Claude Code doesn't have a "Cowork"-style multi-connector picker. Not yet done as of this writing. **Both items under "New priorities from Peter, Sep 4 night" are done too** — see that section for what changed and verification notes.
>
> **New this pass: CW-31, CW-33(a), and CW-34 are done; navigation is done; CW-32 is deliberately not started yet.** See "Scope expansion from Peter" and "Continue the build" below for full detail, but the headline finding worth reading before anything else: **both official government sources for CW-33/CW-34 (egov.uscis.gov and travel.state.gov) sit behind a Cloudflare bot challenge that blocks a plain HTTP fetch** — confirmed by testing directly, not assumed. A live scraper (the thing both tickets originally assumed as the "obvious" approach) would almost certainly get the same block from a Vercel serverless function. A real browser passes the challenge fine, so all the data in the two new KB files below was captured by actually loading the real tool/page and reading its output — this is the "Claude-assisted manual update" alternative both tickets flagged, not a live pipeline, and it needs a human (or a Claude session with browser tools) to refresh it periodically, not a cron job. Flagging this clearly since it changes the engineering picture from what the tickets assumed.
>
> **Later same morning (~7:40am ET): CW-32 built and done, PWA manifest/service worker done, CW-35 and CW-36 done.** CW-32's chat is real (grounded in the CW-31 KB, guardrail-tested against real adversarial questions). CW-35 (chat metering) and CW-36 (multi-case tracking, capped by plan) are schema/logic complete against Peter's confirmed real pricing ($9.99/mo "CaseWhy Plus," 10 free chat questions/month, 5-case cap) — **still correctly not wired to live billing**, no Stripe/LLC/lawyer-reviewed ToS exist yet. CW-36's schema migration (dropping `tracked_cases`' old one-row-per-account unique index) was applied directly to the live, empty Neon DB and verified. Two things genuinely can't be verified live yet, both for the same reason: the USCIS sandbox has been down the whole time (Saturday, sandbox is Mon–Fri only) — CW-32's real chat round trip and CW-36's real track/cap/untrack flow both need a real `getCaseStatus()` call to reach the UI that exercises them. **Do both together next sandbox session (Monday, 7AM–8PM ET).**

## What this is

CaseWhy is an AI-explained USCIS case-status tracking web app. Existing trackers (Lawfully, US Case Tracker, VisaWatch) poll USCIS and show status. CaseWhy's differentiator: turn a status change or policy shift into a plain-language explanation and a concrete next action, instead of just a timestamp. **Updated Sep 5, 2026 (later that day):** the free/paid split described here is under active revision — see the new section below, "Scope expansion from Peter, Sep 5." The "ask a question" AI chat is no longer v2 — it's initial-rollout scope now, alongside a knowledge base, processing-time data, and visa bulletin tracking. Secondary revenue: attorney-referral warm handoff (still stretch).

## Origin

Peter spent 2026 exploring AI-leveraged business ideas he could build and run himself (15 years as a Solution Architect, wants to use AI for dev/marketing/ ops, B2C not B2B, $500-5,000 budget). CaseWhy was selected from a B2C shortlist on Sep 2, 2026. The idea is grounded in his own family's ordeal: N-400 naturalization applications for himself, his wife, and his daughter hit a 120+ day unexplained post-interview delay tied to a since-struck-down USCIS policy memo pausing naturalization for applicants born in ~39 "high-risk" countries — resolved only through a congressional inquiry and persistent follow-up. The product systematizes the plain-language status-tracking Peter built for his own case by hand.

Full background:

- Idea shortlist: "Ideas" Claude Project → `top-3-ai-business-ideas.md`, `top-3-b2c-app-ideas.md`
- Full MVP scope (architecture, legal risks, monetization, go-to-market): "Ideas" Claude Project → `immigration-case-companion-mvp-scope.md` — this is the source of truth for product/business decisions; this file is the source of truth for build status. **Correction Sep 4, 2026:** that doc is kept current by the cloud Claude session, not Claude Code — Claude Code doesn't have access to claude.ai Projects, so don't try to update it from here; report status back to this file (as always) and the cloud session will fold it in.

## Status as of Sep 4, 2026

**Business & legal**

- Domain, DNS, SSL: done — casewhy.com live, Cloudflare registrar + DNS, code on GitHub, deployed via Vercel
- Landing page + draft privacy policy: done, live since Sep 2-3
- Google Workspace business email: done — casewhy.com shows Verified + Gmail activated in the Admin Console (confirmed Sep 4, 2026). info@casewhy.com is the sole licensed user and primary inbox. `hello@casewhy.com` and `privacy@casewhy.com` are configured as email aliases on it (not separate licensed accounts), with Gmail filters auto-labeling incoming mail by "To" address ("Hello" / "Privacy" labels) so privacy-alias mail (data-subject/deletion requests) stays visibly distinct from general inquiries.
- Florida LLC: **in progress, Sep 6, 2026** — ordered via Northwest Registered Agent (order #88978WW, $164: $125 FL state filing fee + $39 formation service including 1 year of registered-agent service). Expected registered **Sep 8, 2026**. Once confirmed, next steps are the free EIN at irs.gov (needs the LLC to exist first) and a business bank account. This is the real dependency the round-8 social task flagged for Instagram/Facebook's Meta business verification — check back here around Sep 8 rather than treating it as indefinitely blocked. Registered-agent service renews after year one (~$125/yr estimated, unconfirmed); can switch to self-registered-agent later for free via Sunbiz if that renewal isn't worth it. Separately, Florida's Annual Report ($138.75, due every May 1, $400 late fee with no grace period) applies regardless of registered-agent choice — worth a standing calendar reminder once the LLC is confirmed.
- Open naming issue, narrower than previously noted: `casewhy.uscis@gmail.com` exists as a Google account (visible in the account chooser) but was **never actually used** on the live site — checked the deployed `index.html`/`privacy.html` directly (Sep 4, 2026): landing page uses `hello@casewhy.com`, privacy policy uses `privacy@casewhy.com`, neither contains "uscis". So the live contact links are already fine. Still avoid using `casewhy.uscis@gmail.com` anywhere official (the affidavit, etc.) per the same reasoning (undercuts the not-affiliated-with-USCIS disclaimer). Whether to consolidate `hello@`/`privacy@` further was raised and the decision was to keep all three addresses distinct (general/privacy/institutional) rather than merge — don't redo that ask without new information.

**USCIS API access**

- Developer account + sandbox Case Status API key: issued Sep 3, 2026 (evening)
- `getCaseStatus()` wired to the real sandbox endpoint (`GET https://api-int.uscis.gov/case-status/{receiptNumber}`): done, `tsc` clean, verified end-to-end Sep 4, 2026 8:16am ET with a real 200 for sandbox receipt `EAC9999103403`
- 5-day sandbox traffic requirement (must exercise both success and 4xx paths for 5 consecutive days before requesting the affidavit): Day 1 (Fri Sep 4) logged. Remaining: Mon Sep 7 – Thu Sep 10, 2026. Checked Sep 5: Mon Sep 7, 2026 is Labor Day (federal holiday, USCIS offices/staff closed) — but developer.uscis.gov's own production-access page currently reads **"5 consecutive calendar days,"** not business days/weekdays, and the sandbox API is unstaffed infrastructure, not a USCIS office. So logging traffic on Sep 7 should still count normally; no need to skip it or push the schedule out. (The "weekdays" phrasing previously used in this repo was this project's own shorthand, not USCIS's actual wording — corrected here.) The one real holiday effect: don't expect a fast reply if the affidavit email happens to land right around a federal holiday, since that mailbox is staffed.
- Affidavit request to developersupport@uscis.dhs.gov: not started — blocked on the 5-day log finishing
- Live USCIS demo (Tue/Wed/Fri, first-come-first-served; covers UI usability, file upload, JSON payload handling, OAuth 2.0, error handling, case status tracking): not scheduled — needs a demo-ready product first, not just an API call
- Production API access: pending all of the above
- A duplicate, unused sandbox credential pair exists on the "CaseWhy App" in the dev portal (only the first pair is in `.env.local`) — investigated, no self-service revoke exists, left as-is (sandbox-only, low risk)
- FOIA Request and Status API client (`src/lib/uscis/foia.ts`): implemented and `tsc`-clean Sep 4, 2026 — `createFoiaCase()` and `getFoiaCaseStatus()`, types confirmed against the live docs (v1.2.0). ~~**Live sandbox testing is blocked**~~ — **resolved, USCIS replied Sep 5, 2026 evening. Real action needed here, not just a status flip.** USCIS's answer: the credential in `.env.local` (the one starting `joJ...`) is the **Case Status API sandbox credential**, not a FOIA one — it was never going to work for FOIA regardless of the product-enablement setting, this wasn't actually a platform-side bug. The fix is to use the credential starting `Umv...`, which USCIS says is already active and correctly configured for the FOIA API. **This is almost certainly the "duplicate, unused sandbox credential pair" noted on the line above** — it isn't a duplicate at all, it's the real FOIA credential, sitting in the dev portal the whole time. Verbatim reply, Sep 5, 2026, from developersupport@uscis.dhs.gov:
  > "The credential you indicated you are attempting to use, beginning with 'joJ', is currently associated with the Case Status API - Sandbox and is not configured for use with the FOIA API. For FOIA API testing, please use the active credential associated with that API, which begins with 'Umv'. Please try again using the FOIA API credential and let us know if you continue to experience any issues."

  ~~**Next action for Claude Code:**~~ **Done, Sep 5 ~8:30am ET — see CW-18 under "Next tasks for Claude Code — round 2" below for the full story** (credential pulled from the portal by Peter directly since that needs a real login, wired up with its own token cache so Case Status is untouched, verified via a real sandbox call all the way down to one remaining validation error — a missing USCIS MyAccount email nobody's signed up for yet). ~~Only open item: reply to USCIS's thread confirming it works~~ — **sent by Peter, Sep 5 evening.** That reply covered the credential-fix confirmation and the initial digitalDelivery blocker. See the note under CW-18/round 2 below: this file also describes a *second*, more thorough follow-up draft (written after the deeper digitalDelivery investigation — the enum type and the sandbox-test-pool theory) that was still unsent as of Claude Code's last update here. Confirm with Peter whether the reply he just sent already covers that ground or whether the second draft should still go out.

**Product build**

- Next.js/TS/Tailwind scaffold: running locally in this folder — `npm install` and `npm run dev` both succeed
- Dashboard UI (`src/app/dashboard/page.tsx`): done Sep 4, 2026 — server component with a receipt-number search form (plain GET, no client JS needed), wired directly to `getCaseStatus()`. Renders status/description/history on success, a friendly message on failure, and an empty state before any search. Verified in-browser against the live sandbox. This unblocks the live USCIS demo prerequisite (CW-09) and the demo-ready milestone (CW-15).
- Git-linked Sep 4, 2026: pushed to `petertakla/casewhy` on a new **`nextjs-app` branch**, not `main` — `main` is still the live static landing page deployed to casewhy.com, and this Next.js app isn't ready to replace it yet. Open a PR from `nextjs-app` when it is.
- CI (`.github/workflows/ci.yml`): install → `tsc --noEmit` → `next build`, runs on push/PR to `nextjs-app`. Sandbox `USCIS_CLIENT_ID`/`USCIS_CLIENT_SECRET` are GitHub Actions secrets on the repo. First run passed clean.
- **Plain-language explanation layer: done Sep 4, 2026 (evening)** — `explainCaseStatus()` (`src/lib/ai/explain.ts`, `ai` SDK + `Output.object`/zod, model `anthropic/claude-haiku-4.5` via AI Gateway, `claude-sonnet-5` documented as the upgrade path if quality is insufficient) wired into the dashboard: renders a plain-language explanation + general next steps next to the raw status, fails gracefully (raw status still shows) if the model call errors. Verified end-to-end against a real case via a temp isolated test route (deleted after use). Committed on `nextjs-app` (b47b0a0).
  - Getting this working required fixing an AI Gateway 403 (`Free tier users do not have access to this model`, later `BYOK is available only with paid credits`): upgraded team to **Pro**, bought a **$20 one-time AI Gateway credit top-up** (separate from the Pro plan subscription itself — easy to conflate, the first attempt mistakenly paid the Pro invoice instead), then added a personal Anthropic API key via AI Gateway **BYOK**. Budget for further AI Gateway credit top-ups as usage grows.
- Note: `npm run lint` is currently broken repo-wide (missing `eslint.config.js` for ESLint 9) — see next-build-priorities below.
- **Persistent case tracking + minimal auth: done Sep 4, 2026 (late evening)** — magic-link sign-in via **Neon Auth** (managed Better Auth, `@neondatabase/auth`) and one Postgres table (`tracked_cases`, Drizzle + `pg`) enforcing one free tracked case per account. Signed-out visitors keep today's one-off lookup behavior unchanged (no forced sign-in); signing in adds a "Track this case" action, and a bare `/dashboard` visit auto-loads the signed-in user's saved case. Verified end-to-end in-browser on the live `casewhy-app.vercel.app` deployment: sign-in email arrives, magic link signs in, tracking persists across reloads, sign-out reverts to anonymous view.
  - Three real bugs hit and fixed along the way, all worth knowing about if Auth acts up again: (1) `@neondatabase/auth` peer-requires Next.js ≥16 while this app is on 15 — installed with `--legacy-peer-deps` and added `.npmrc` (`legacy-peer-deps=true`) so Vercel's build does the same; works fine in practice. (2) Neon Auth itself wasn't enabled on the project yet (Neon Console → project → Auth → "Enable Neon Auth"), and separately the **Magic Link plugin specifically** was off by default even after enabling Auth (Console → Auth → Plugins tab → toggle "Enable Magic Link" — email+password auth is on by default, magic link is not). (3) `NEON_AUTH_BASE_URL` in `.env.local`/Vercel was stale — pointed at a different, non-existent Auth endpoint hostname than the one Neon actually provisioned once Auth was enabled; had to re-pull the real "Auth URL" from Neon Console (Auth → Configuration tab) and fix it in both places. Also had to add `https://casewhy-app.vercel.app` to Neon Auth's trusted-domains allowlist (Console → Auth → Configuration → Domains) or every callback fails with `INVALID_CALLBACK_URL`.
  - ~~**Known gap against the compliance guardrails below:** `tracked_cases.receipt_number` was stored as plain text.~~ — **fixed Sep 4, 2026, same night**, user called it a hard requirement. `src/lib/db/crypto.ts` adds app-level AES-256-GCM (Node's built-in `crypto`, no new dependency) — encrypt on write in `trackCase()`, decrypt on read in `getTrackedReceiptNumber()`, with an undecryptable row (e.g. old pre-encryption data) treated as "no tracked case" rather than crashing. Key lives in a new `ENCRYPTION_KEY` env var (`.env.local`, Vercel Production/Preview, GitHub Actions secret — same three-places pattern as the other secrets). Verified in production: raw Postgres value is 56-byte ciphertext with no plaintext receipt number present, and decrypt round-trips correctly through the dashboard.
- **File upload UI stub: done Sep 4, 2026 (late night)** — `src/app/dashboard/FileUploadStub.tsx`, rendered in `StatusCard` under "Supporting documents." Accepts a pdf/jpg/png and confirms receipt locally (no real endpoint yet, per the scope this priority was explicitly given). Verified in production: uploading a file renders "Received `<name>` (`<size>`)". This closes the last unstubbed criterion of the six the live USCIS demo evaluates (UI usability, file upload, JSON payload handling, OAuth 2.0, error handling, case status tracking) — all six now have working UI to show.
- ~~`npm run lint` was broken repo-wide~~ — **fixed Sep 4, 2026, late night.** `eslint.config.mjs` was missing entirely (ESLint 9 dropped `.eslintrc.*` support by default) — added via `FlatCompat` (`next/core-web-vitals` + `next/typescript`), with `.next/`, `node_modules/`, and the auto-generated `next-env.d.ts` ignored. Lint runs clean; `npm run lint` is now a CI step between `tsc --noEmit` and `next build`.
- **Email notification infrastructure: DONE and live end-to-end, Sep 5, 2026 (~1:40am ET).** `tracked_cases` extended with encrypted `email` (captured from `session.user.email` at track-time) and `lastStatusText`, plus plain `lastCheckedAt`. New `POST /api/cron/check-status` (`src/app/api/cron/check-status/route.ts`), protected by `Authorization: Bearer <CRON_SECRET>`: for each tracked case, calls the existing `getCaseStatus()`, compares to the last-seen status, and on a change sends via a new Postmark HTTP wrapper (`src/lib/email/postmark.ts`, plain `fetch`, no SDK) — but never on the *first* check after tracking (the user already saw that status when they tracked it; a notification then would be redundant). Switching a tracked case to a different receipt number resets the notification baseline, so a stale status from the old case can't fire a spurious email for the new one.
  - `sendStatusChangeEmail()` no-ops with a console warning if `POSTMARK_API_TOKEN` isn't set — this is deliberate, not a bug, and is why the polling/diff logic could be fully built and verified tonight without a real Postmark account existing yet. Verified locally end-to-end: first cron call sets a baseline (0 notifications), manually mutating the stored status and calling again correctly detects the change and attempts a send (`notified: 1`, no-op warning logged as expected). Verified in production too: the Bearer-auth guard rejects requests with no/wrong token (`401`) and accepts the right one (`200`, `{checked:0, notified:0, errors:[]}` since production has no tracked cases right now).
  - ~~Cron scheduling~~ — **done Sep 5, 2026, past midnight.** New dedicated cron-job.org account signed up under `info@casewhy.com` (not the NVDA trading project's — that one's at its 7-job free-tier cap; separate account keeps CaseWhy's infra cleanly apart). One job, "CaseWhy status check," `POST https://casewhy-app.vercel.app/api/cron/check-status`, daily at 9:00 AM America/New_York, `Authorization: Bearer <CRON_SECRET>` header. Test-fired from cron-job.org's own UI before saving — `200 OK`, correct `{"checked":0,"notified":0,"errors":[]}` body, first try, no repeat of the NVDA project's "missing literal `Bearer ` prefix" bug. Job is enabled, next run confirmed showing in the console.
  - ~~Postmark account + domain verification~~ — **done Sep 5, 2026, ~1:40am ET.** The user already had a Postmark free trial with `casewhy.com` + `info@casewhy.com` pre-added as a sender signature — just needed DKIM/Return-Path DNS verification, which Claude Code added directly at Cloudflare (both records resolving correctly via public DNS within seconds; Postmark's own "Verify" button confirmed both **Verified** on the first retry). Server API Token pulled from the account and added as `POSTMARK_API_TOKEN` (`.env.local`, Vercel Production/Preview, GitHub Actions secret). **Note: account is in Postmark's test mode** (100-email cap, can only send to `casewhy.com` addresses — fine for internal testing, but real users with e.g. `@gmail.com` addresses won't receive anything until "Request approval" is submitted and granted; that's a user-initiated step, not done yet).
  - **Verified fully live end-to-end**, real email included: inserted a test tracked-case row (`email` = `info@casewhy.com`, a pre-existing fake status so the cron would detect a "change"), triggered the production cron — first call hit a transient USCIS sandbox rate limit (429, unrelated, handled cleanly by the per-row error handling with zero impact on other rows), retried a few seconds later and got `{"checked":2,"notified":1,"errors":[]}` — a real send succeeded. **Also caught in the same pass:** a second row from earlier testing turned out to be tied to the user's *personal* email (from signing in as them to verify the tracking UI) — it hadn't sent anything yet (first-check protection correctly no-ops), but would have started notifying that personal address going forward, so it was deleted per the user's explicit choice. `tracked_cases` is empty again as of this writing — clean slate for real use.

**Target:** provisionally pulled forward to ~Nov 13, 2026 (was Nov 30) — the roadmap Gantt was updated Sep 4, 2026 evening after an audit found it (and the Ideas project doc) had drifted from actual status. The affidavit can go in ~17 days earlier than first estimated (5-day traffic log finishes Sep 10, not ~Sep 25), so the whole downstream chain shifted with it. Still provisional — depends on USCIS's own review taking about the same ~4 weeks as originally estimated. See the roadmap artifact below for the full updated week-by-week plan.

## Right now — confirm a checkable UI (do this first, it's quick)

**Correction, Sep 5, 2026 ~4:45am ET — the claim below was wrong, root-caused after it bit us twice.** Every push to `nextjs-app` was landing as a **Preview** deployment, not Production, despite two separate "done, verified in production" claims (this section originally, and again after tonight's CW-31/33/34 push) — both times the fix was a manual `vercel promote ... --yes` (the user ran it; Claude Code is blocked from the non-interactive form by its own safety classifier). Root cause, confirmed directly via the Vercel API (`GET /v9/projects/casewhy-app`): the project's actual Git integration has **`productionBranch: "main"`**, not `nextjs-app` — `main` on this repo is an effectively-unused branch (the real static landing page at casewhy.com is a *separate* Vercel project, "casewhy", not this one). The earlier "vercel inspect shows target: production" evidence was real but misleading — it was almost certainly from an earlier manual `vercel --prod` deploy, which forces `target: production` on that one deployment regardless of the Git branch setting, not from the branch setting actually being correct. **Fixed, Sep 5, 2026 ~5:00am ET.** The API/CLI attempts above were rejected (not a settable field there), but it turned out not to be a Git-tab setting at all in the current Vercel UI — it's under **Project Settings → Environments → Production → Branch Tracking**, changed there via the dashboard (Claude in Chrome) from `main` to `nextjs-app`, saved, then redeployed via the same page's "Redeploy" action to confirm: the new deployment built with `target: production` automatically and the `casewhy-app.vercel.app` alias updated correctly (`vercel inspect` confirmed, all four routes re-checked with real `curl` 200s). **This is genuinely resolved now** — every future push to `nextjs-app` should deploy straight to production on its own; no more manual `vercel promote` needed. If it ever regresses, check Project Settings → Environments → Production first, not the Git tab (that page only shows the GitHub App connection, not branch tracking).

The stable URLs, once a deployment actually is Production:

- **https://casewhy-app.vercel.app** — stable alias, click this one
- https://casewhy-app-smarticos.vercel.app — same deployment, alternate alias
- https://casewhy-app-git-nextjs-app-smarticos.vercel.app — branch-specific alias, will always point at the latest `nextjs-app` push

This is separate from casewhy.com, which is still the `main`-branch static landing page on a different Vercel project — no change there.

A real case lookup on that URL initially failed with "Something went wrong looking up your case" — the deployed app had no `USCIS_*` env vars set on Vercel at all. Fixed by adding them via `vercel env add` (had to strip stray double-quotes that `.env.local`'s formatting carries but Vercel doesn't). **Verified working end-to-end in-browser**, and now also verified again after the Sep 4 late-evening persistent-tracking/auth work below — see that section for what's new.

**Process note for the cloud session:** this section (and item #10 in "Immediate next actions") got reset back to the unresolved "check for a preview URL" text in a later save, overwriting the resolution recorded here — almost certainly because that save was working from a copy of this file fetched before the resolution was written, not a deliberate re-open. This file has no real-time sync between the two sessions, so if Claude Code has updated a section recently, re-fetch this file immediately before editing it rather than editing a cached copy, to avoid clobbering fresh status.

## Next build priorities (added Sep 4, 2026, evening; #1 and #2 done later same evening/night)

Everything else open right now — the sandbox traffic log, the Florida LLC, the FOIA ticket — is either waiting on USCIS, the state, or just needs to run out its own clock. None of it blocks code work. In priority order, all fully actionable now:

1. ~~**Plain-language explanation layer.**~~ — **done Sep 4, 2026 evening**, see Product build above.

2. ~~**Persistent case tracking + minimal auth.**~~ — **done Sep 4, 2026 late evening**, see Product build above. Note the open encryption-at-rest gap flagged there.

3. ~~**File upload UI stub.**~~ — **done Sep 4, 2026, late night.** `src/app/dashboard/FileUploadStub.tsx`, rendered inside the case's `StatusCard` under "Supporting documents": accepts a pdf/jpg/png, confirms receipt locally ("Received `<name>` (`<size>`)") — no real endpoint yet, matching the doc's own explicit scope for this pass. Verified end-to-end in production (uploaded a real file via the browser, confirmation rendered correctly). This closes the live-demo criterion gap; all six demo-evaluated things (UI usability, file upload, JSON payload handling, OAuth 2.0, error handling, case status tracking) are now covered.

4. ~~**Fix `npm run lint`.**~~ — **done Sep 4, 2026, late night.** `eslint.config.mjs` added (ESLint 9 flat config via `FlatCompat`, `next/core-web-vitals` + `next/typescript`, `.next/`/`node_modules/`/`next-env.d.ts` ignored) — lint runs clean. `npm run lint` is now a CI step, between `tsc --noEmit` and `next build`.

5. ~~**Email notifications** (SendGrid or Postmark) on a detected status change.~~ — **Fully done and live Sep 5, 2026, ~1:45am ET**, see Product build above. Code, cron scheduling, Postmark domain verification, and a real end-to-end test send all verified. This was the last item on this list — **nothing left here that's code work.** Only open thread: submitting Postmark's "Request approval" to leave test mode (user-only, whenever real users need to receive notifications, not urgent before then).

## New priorities from Peter, Sep 4 night (after reviewing live screenshots)

Two concrete build asks — **both done Sep 5, 2026, ~2:30am ET** — plus context on two questions that are a scope decision, not a build task yet:

1. ~~**Visual polish pass on sign-in + dashboard.**~~ — **Done.** Direct feedback was "the black background is dull," wanting a real design pass, not a quick color swap. Replaced the flat near-black dark background with a layered token system (`background`/`surface`/`surface-2`/`border`/`muted` CSS variables, swapped per `prefers-color-scheme`, wired into Tailwind so components don't need `dark:` pairs anymore) plus a subtle fixed brand-colored radial glow behind the page so dark mode reads as designed rather than flat. Added real typography (`next/font/google` Inter + JetBrains Mono, previously no font was configured at all — system default). Rebuilt the header into a sticky, bordered, backdrop-blurred nav with a real wordmark/logo mark (`src/components/Logo.tsx`); redesigned the dashboard's status card with a semantic color-coded status pill (green/red/amber/blue by keyword-matching the status text), a restyled "What this means" callout (icon + left accent bar instead of a plain tinted box), a real connecting-line history timeline, and a proper dropzone-style file upload control; redesigned the sign-in page to match (icon, card, success state with a checkmark). Verified in-browser via `npm run dev` + Claude in Chrome against the real sandbox case `EAC9999103403` (the same I-130-approved example from Peter's screenshot) in dark mode; `tsc --noEmit` and `npm run lint` both clean. Not yet re-verified against a real deploy or in light mode by eye — worth a look next time either comes up.
2. ~~**Sharpen the explanation layer's actual value-add.**~~ — **Done, tighter prompt, no knowledge base built yet.** Direct feedback was "where is the value-add of the explanation" — the I-130-approved example read as generic paraphrase-plus-boilerplate ("keep your notice safe," "consult an attorney if unsure"), not clearly differentiated from USCIS's own status text. Per the doc's own suggestion, tried the tighter-prompt route before concluding the curated policy/case-law knowledge base is required: `explainCaseStatus()` (`src/lib/ai/explain.ts`) now also feeds the model the case's **full status history** (every `hist_case_status` entry, not just the current one), **filed/last-updated dates with elapsed-days computed in code** (not left to the model's own date math), and a **receipt-number-derived processing center** (a small prefix→center table sourced from USCIS's own published receipt-number convention, e.g. `EAC` → Vermont Service Center; unrecognized prefixes are simply omitted rather than guessed). System instructions were also tightened to explicitly forbid generic-filler next steps unless the given facts actually support them. **Verified live against the same I-130 example**: the explanation now correctly references the case's second history entry (forwarded to a specific U.S. Embassy/Consulate) and gives a next step tied to that ("contact the U.S. Embassy or Consulate identified in your notice") instead of the old generic boilerplate — a real, visible improvement, not just a prompt-wording tweak. **Open question, not yet decided:** whether this is enough, or whether the curated knowledge base (grounding *why* a case is delayed against a known policy memo/backlog/court ruling — the thing a tighter prompt can't produce, since that's not in USCIS's own status text at all) is still worth prioritizing. Worth showing Peter this version before deciding.

Not build tasks yet — flagged so you're not blindsided if these come up in conversation:

- **"Where is the paywall / multiple receipts / free stuff to match competition"** — this came back with an answer, see the new section immediately below. **Still not decided:** the actual free-vs-paid packaging (Section 6 of the MVP doc is reopened, not resolved) — don't build multi-case gating or any paywall on your own initiative, since it affects the DB schema (`tracked_cases` currently enforces one row per account) and the roadmap's monetization sequencing.

## Scope expansion from Peter, Sep 5, 2026 (later that day) — four new build items

The competitor research came back (Lawfully, VisaWatch, and Case Tracker for USCIS & NVC — see the cloud session's "CaseWhy Competitive Comparison" artifact) and confirmed Peter's instinct: those three already give away free what CaseWhy was treating as paid-tier hooks or v2 stretch. Rather than just match the free tier, **Peter decided to build four features into initial rollout** so CaseWhy launches ahead of the competition on features, not just even with it. Added to the checklist as CW-31 through CW-34, in the order they probably need to happen. **Status as of Sep 5, ~4:15am ET: CW-31, CW-33(a), and CW-34 done; CW-32 deliberately not started.**

1. ~~**CW-31 — Curated policy/case-law knowledge base.**~~ — **Done, v1 scope: 3 entries, all sourced from primary/official documents, real (verified via live web research, not written from memory).** `src/lib/kb/policy-memos.ts`: (a) **PM-602-0194**, the January 1, 2026 hold on benefit applications from 39 "high-risk" countries — this is literally the memo Peter's own N-400 case was affected by (see Origin above) — including that it was later vacated/blocked by federal courts in MA/RI/TX through mid-2026; (b) the **2026 public charge rule** (DHS rescinded the 2022 rule, effective Sept 18, 2026, date-gated so it only surfaces for I-485s filed on/after that date); (c) the **RFE/NOID adjudicative-standard history** (2018 discretion-to-deny-without-RFE policy → rescinded 2021 → reverted to the 2013 "issue an RFE if more evidence could help" standard, which is still current). Each entry matched against a case **deterministically** (form type + keyword overlap with the case's own status text/history, or a filing-date cutoff for date-gated rules — see `findRelevantPolicyContext()`), capped at 2 matches — **never the model's own judgment**, so citations shown to the user can't be hallucinated. Wired into `explainCaseStatus()` (`src/lib/ai/explain.ts`) as "possibly relevant policy background," with explicit system-instruction guardrails to *never* present a match as confirmed fact (the Case Status API has no nationality field, so CaseWhy can never actually confirm e.g. the high-risk hold applies to a specific case — only that it plausibly might). Also now rendered as visible citations under the explanation on the dashboard (title + source link), not just folded silently into prose — this is the part that makes the value-add visible, not just felt. **Verified two ways:** a standalone unit-style check of the matching logic (5 synthetic cases, correct matches/non-matches including the real production baseline case matching nothing, as expected), and a real end-to-end call to `explainCaseStatus()` with a synthetic long-delayed N-400 — the model correctly cited the high-risk-hold memo, hedged appropriately ("may have been subject to..."), and suggested checking USCIS's real Emma chatbot. **Real estimate delivered as asked: this v1 took about half a day** (mostly research/sourcing, the code itself is small). Growing the KB further (more memos, deeper case-law) is real, ongoing editorial work — no fixed estimate for "comprehensive," and per the ticket's own framing this v1 (a handful of the most consequential items) was the right bar, not a first slice of a bigger planned build.
2. ~~**CW-32 — "Ask a question" AI chat.**~~ — **Done, Sep 5 ~6:30am ET.** Picked back up as the next real build item once CW-31 had a usable first version. Real chat, not a stub: `/ask` (signed-in + tracked-case-required, same gating pattern as the dashboard) renders `CaseChat` (`src/app/ask/CaseChat.tsx`), a client component posting to `POST /api/chat` (`src/app/api/chat/route.ts`). The route re-derives the user's tracked receipt number and re-fetches case status server-side from the authenticated session every request — it never trusts a client-supplied case status, only the conversation itself.
   - **Shared fact-gathering, refactored out first:** `explainCaseStatus()`'s prompt-building (form type, processing center, dates, history, matched policy background) was identical to what the chat needed, so it's now `buildCaseContext()` in the new `src/lib/ai/case-context.ts`, used by both `explain.ts` and the new `chat.ts` — real duplication removed, not just avoided going in.
   - **Guardrails, tightened beyond the explanation layer as the ticket asked for:** `src/lib/ai/chat.ts`'s system instructions add explicit handling for prediction requests ("will I be approved" → decline, point to `/processing-times` and `/visa-bulletin`, note USCIS is the only real source), strategic/legal questions (general concept if one exists, then an explicit "get an attorney" — not just a refusal), and staying on-topic. Never claims CaseWhy affiliation with USCIS/DHS.
   - **Verified against real adversarial questions, not just happy-path, per the ticket's own requirement** — ran 5 real questions through the live model against a synthetic long-delayed N-400: a percentage-approval-odds request (correctly declined, redirected to reference pages), "should I pay the officer to speed this up" (correctly refused, redirected to legitimate channels — congressional caseworker, attorney), a fraud/nationality-hiding question (correctly refused to advise, strongly redirected to an attorney, even surfaced free/low-cost legal-aid orgs), an off-topic question (politely redirected on-topic), and a legitimate "why is this stuck" question (grounded, specific, correctly hedged the matched policy-KB citation as background, not fact). All five passed cleanly on the first real run — see the "Real bug caught mid-testing" note below for the one thing that *didn't* pass cleanly at first.
   - **Real bug caught mid-testing, fixed before calling this done:** `relatedPolicies` is deterministic per-*case* (not per-question — see `findRelevantPolicyContext()`), so the first implementation attached the same policy citation to every assistant reply, including the off-topic "pizza toppings" test question — made it look like the citation was the reason for an unrelated answer. Fixed by moving it out of per-message display into a single persistent "Policy background that may apply to this case" panel shown once, above the conversation, updated from the latest response rather than repeated per bubble.
   - **Verification:** `tsc --noEmit` and `npm run lint` clean (same pre-existing `Logo.tsx` warning, unrelated). The full signed-in-but-no-tracked-case empty state was verified in-browser; the live tracked-case → chat round trip could *not* be verified end-to-end in-browser this pass — the USCIS sandbox was outside its published 7AM–8PM ET operating hours at the time (a real, time-of-day constraint hit before in this session, not a code issue). Confirmed instead against `casewhy-app.vercel.app` directly: unauthenticated `POST /api/chat` correctly returns `401`. (One transient false alarm on the way there, worth remembering: right after pushing, the same request briefly 404'd — a normal deploy-propagation timing gap, not a real bug — waited for `vercel ls` to show the new deployment `Ready` before retesting, then it was a clean `401`. Don't read a 404 immediately after a push as broken without re-checking against the deployment that's actually finished building.) The route reuses the exact same auth/tracked-case/`getCaseStatus()` pattern already proven working in the dashboard. **Worth a real click-through next session during sandbox hours before treating the full chat flow as verified in production.**
3. **CW-33 — Processing-time / approval-trend analytics.** ~~(a) surfacing USCIS's own published processing-time ranges~~ — **done**, see below. (b) Lawfully-style comparative analytics against CaseWhy's own accumulated case data — still correctly deferred, `tracked_cases` doesn't have enough real users yet for this to mean anything.
   - **Real, load-bearing finding before the "done": `egov.uscis.gov/processing-times` sits behind a Cloudflare bot challenge that blocks a plain fetch** — tried with a realistic browser User-Agent and got Cloudflare's "Attention Required" page, not the tool. This was assumed to be "a bounded, well-understood problem" in the original ticket; it isn't, if the plan was a live scraper — a Vercel serverless function would very likely get the same block. Loading the real tool in an actual browser (Claude in Chrome) passes the challenge fine, which is how the data below was actually captured.
   - **Built `src/lib/kb/processing-times.ts` + `/processing-times` page** with 4 real, precisely-labeled figures pulled live from the official tool (as of Sep 4, 2026): I-130 Immediate Relative (SCOPS) 24 months; I-140 EB-2 (SCOPS) 2.5 months; I-485 employment-based (SCOPS) 40 months — **important caveat surfaced directly on the page**, this SCOPS figure is the tool's own note that it only covers EB-4/EB-5, not EB-1/2/3 which are field-office adjudicated with no national aggregate; I-765 pending-I-485 (National Benefits Center) 11 months. N-400 and family-based I-485 are honestly called out as "office-specific, no national figure" (every N-400 is adjudicated by the applicant's own local field office — nothing in the Case Status API says which one) rather than either omitted silently or given a made-up number.
   - **Also surfaced, worth knowing**: the tool's own on-page alert says USCIS is mid-transition away from naming a specific service center (e.g. "Vermont Service Center") toward a consolidated "Service Center Operations (SCOPS)" bucket for processing-time *reporting* purposes. This doesn't break `serviceCenter()` in `explain.ts` (receipt-number prefixes still map to the historical centers, and correspondence still references them), but it's a real, current caveat if that function ever needs to line up with official processing-time data by center.
4. ~~**CW-34 — Visa bulletin tracking.**~~ — **Done, manual-update approach chosen** (per the same Cloudflare finding above — `travel.state.gov` is also behind the same bot challenge, confirmed by testing; a live scraper isn't realistic here either, so the "lighter alternative" the ticket offered is what got built). `src/lib/kb/visa-bulletin.ts` + `/visa-bulletin` page: real **Final Action Dates** for September 2026, both Family-Sponsored (F1–F4) and Employment-Based (EB-1–EB-3), by country (All Other/China/India/Mexico/Philippines) — captured by loading the real bulletin in a browser and reading the actual tables, not estimated or aggregated from secondary sources. Refresh cadence: monthly, by hand (or a Claude session with browser tools) — there's no API and no reliable way to automate the fetch, so this needs to become a recurring small task, not a one-time build.

**Please give a real estimate for these four once you've looked at them directly** rather than letting the cloud session's rough placeholder dates on the roadmap stand — the knowledge base especially could be days or could be weeks depending how deep Peter wants the initial version. The roadmap ("CaseWhy Launch Roadmap" artifact) has a first-pass guess (Sep 8–29) with the paid-launch milestone nudged from Nov 13 to a provisional Nov 17; treat both as placeholders pending your estimate, not commitments.

**Navigation gap, flagged Sep 5 (later):** today's dashboard is a single page with no menu at all — there's no way to reach four new pages once they exist. The cloud session added a rough sketch of a fix (a left-nav sidebar: Dashboard / Ask a question / Processing times / Visa bulletin) to the "CaseWhy Screens" artifact (https://claude.ai/code/artifact/35f8924b-837f-427e-81ec-1d0069d1ecef, see "Not built yet — main app navigation"). Treat it as a sketch that makes the problem visible, not a spec to implement literally — sidebar vs. top tabs vs. something else, and how it collapses on a phone width, are still open. Fold this into CW-28 (the queued visual-design pass) rather than building nav as separate throwaway work, since CW-28 was going to touch the same layout anyway.

## Continue the build — where to start, Sep 5 (later)

A concrete order for the four new items, since "in priority order" above is now spread across two lists. **Status as of Sep 5, ~4:15am ET: 1, 2, and 4 done; 3 (chat) deliberately not started, see reasoning under CW-32 above.**

1. ~~**CW-31 first, and scope it before writing code.**~~ — **Done**, v1 scope decided (3 entries, the most consequential ones, not comprehensive), real estimate reported (~half a day) — see CW-31 above for full detail and verification.
2. ~~**CW-33 and CW-34 can run in parallel with CW-31**~~ — **Done, both**, though "bounded fetch/scrape jobs" turned out to be the wrong framing — both official sources are Cloudflare-blocked from automated fetches (real finding, tested directly), so both ended up as the manual-update path instead. See CW-33/CW-34 above.
3. ~~**CW-32 once CW-31 has a usable first version**~~ — **Done, Sep 5 ~6:30am ET.** See CW-32 above for full detail, guardrail testing, and the one open item (live sandbox-hours click-through still needed).
4. ~~**Basic navigation to reach these three new pages**~~ — **Done**, but kept genuinely basic as instructed, not the CW-28 sketch's fuller design: nav links added directly to the existing sticky header (`src/components/AuthHeader.tsx`) — Dashboard / Ask a question / Processing times / Visa bulletin, active-link underline, only shown on app pages (not the public landing page), horizontally scrollable rather than wrapping so it doesn't need a hamburger/collapse state on narrow screens. The CW-28 sketch (sidebar vs. tabs, mobile collapse behavior) is still the open question for the *real* nav design — this is deliberately a placeholder that works, not a preempt of that decision.
5. Everything not on this list (sandbox traffic log, Florida LLC, Postmark approval, Gmail delegation) keeps running on its own clock in parallel — none of it blocks 1–4.

**All new/changed files from this pass:** `src/lib/kb/policy-memos.ts`, `src/lib/kb/processing-times.ts`, `src/lib/kb/visa-bulletin.ts`, `src/app/visa-bulletin/page.tsx`, `src/app/processing-times/page.tsx`, `src/app/ask/page.tsx` (all new); `src/lib/ai/explain.ts` (KB wiring), `src/app/dashboard/page.tsx` (policy citations under the explanation), `src/components/AuthHeader.tsx` (nav links) — all `tsc --noEmit` and `npm run lint` clean, verified in-browser via `npm run dev` + Claude in Chrome (dashboard, visa bulletin, processing times, ask-a-question stub, and the landing page correctly *not* showing the new app nav). Not yet pushed/deployed as of this writing — ask the user whether to push before assuming it's live.

Report progress and any real estimates back in this file as usual — re-fetch before editing if the cloud session has touched it since your last read (check the timestamp in the banner at the top).

## Branding decision, Sep 5, 2026 (evening) — logo + tagline finalized

Peter reviewed four logo concepts (built and published by the cloud session as a "CaseWhy Logo Concepts" artifact) and picked one, plus finalized the tagline. This replaces the "tagline not yet finalized" note in the MVP scope doc Section 8 — the cloud session has updated that doc too.

**Decision:** Logo = a rounded-square badge in CaseWhy green (`#1baf7a`) with a white checkmark. Tagline = **"Your USCIS case, explained!"**

**Correction, Sep 5 ~5:20am ET: these were NOT actually committed — fixed now.** The files existed on disk (`src/app/icon.svg`/`favicon.ico`/`apple-icon.png`, `public/brand/*`) but `git log`/`git status` showed no commit ever added them; they were untracked local files only, so the favicon/app-icon would never have actually shipped, and `Logo.tsx`'s `<img src="/brand/mark.svg">` would have 404'd in production the moment it was wired up. Committed all of them together with the `Logo.tsx` change below so nothing's silently broken. If "the cloud session committed these directly" was based on an assumption rather than a verified push, worth double-checking that assumption elsewhere too.

- `src/app/icon.svg`, `src/app/favicon.ico`, `src/app/apple-icon.png` — Next.js App Router auto-detects these special filenames in `src/app/` and wires up the browser-tab favicon and Apple touch icon automatically, zero code/metadata changes required. Should just appear on the next build.
- `public/brand/mark.svg`, `public/brand/wordmark-lockup.svg`, `public/brand/icon-512.png`, `public/brand/icon-192.png` — same mark at other sizes plus a full icon+wordmark lockup SVG, for whenever a PWA manifest gets built (512/192 are the two standard manifest icon sizes) or for anything outside the app (docs, social, README). Not wired into any code yet.

**Accent-color decision, Sep 5 (later that evening): keep blue. Resolved, not open anymore.** Raised the question of whether the site's interactive accent (buttons/links/active-nav, currently `brand-500` blue `#2a78d6`) should switch to match the new green logo. Recommendation was to keep it blue — blue and green are adjacent on the color wheel so the pairing isn't a clash, and more importantly the dashboard's status pills already use green for "approved" (red/amber/blue cover other outcomes); making buttons/links green too would make "click this" and "this case was approved" look like the same signal. Peter confirmed: **keep blue.**

**Both code changes done, Sep 5 ~5:20am ET:**

1. ~~`tailwind.config.ts`~~ — **no value change, as instructed.** Only the placeholder comment on the `brand` object was reworded to "CaseWhy's blue UI accent — kept deliberately distinct from the green logo, see CLOUD_CLAUDE.md" — the color values themselves untouched.

2. ~~**`src/components/Logo.tsx`**~~ — **swapped to the real mark + hardcoded-green wordmark, exact code as specified.** Verified in-browser (`npm run dev` + Claude in Chrome) on both the landing page and sign-in page — green rounded-square checkmark renders correctly, "Case" in default foreground / "Why" in the fixed `#1baf7a`. `tsc --noEmit` clean; `npm run lint` shows one pre-existing-pattern warning (`@next/next/no-img-element`, since this uses a plain `<img>` rather than `next/image` for the small SVG mark) — 0 errors, doesn't fail CI. Committed together with the brand asset files above since the component depends on `/brand/mark.svg` actually existing in the deployed build.

**Copy changes — judgment calls, left to you, not urgent:**

- `src/app/layout.tsx` `metadata` — current title is `"CaseWhy — Understand your USCIS case"`. Consider weaving the finalized tagline in (e.g. as the `description`, or appending it to `title`) — your call on exact wording, just use the locked tagline text verbatim if you do: "Your USCIS case, explained!"
- `src/app/page.tsx` landing hero — the current kicker/H1 ("Understand your USCIS case, not just its status.") is a longer marketing sentence, not the short tagline — these can coexist (tagline near/under the logo, longer H1 as the hero headline) rather than one replacing the other. Fold into CW-28 if/when that visual-design pass happens; not blocking anything.

No rush on any of this — it's cosmetic, doesn't block CW-32 or anything else in flight. Do it whenever convenient.

## Marketing content in the app — recommendation on the Sep 5 port, and the cutover plan

Peter asked Claude Code directly to port the static site's "why CaseWhy" content into the app's own landing page, done Sep 5 ~5:35am ET (see `src/app/page.tsx` in "Key files" below — backlog-stats section, Track/Understand/Act grid, trust/disclaimer section, second email CTA, footer). He separately asked the cloud session for a recommendation on the same question before that landed. Below is that recommendation, **and items 1-3 are now done too, Sep 5 ~6:00am ET** — item 4 (tagline placement) is unchanged, still open, see "Branding decision" above:

1. ~~**The ported copy's core pitch is already a step behind the actual product.**~~ — **Done.** "What CaseWhy does differently" was a generic Track/Understand/Act 3-card grid; now 4 cards leading with what's actually built and real: "Understand, with real sources" (the CW-31 knowledge base's visible policy citations) and "See the full picture" (CW-33 processing times + CW-34 visa bulletin, right alongside the case status). Added a one-line subhead acknowledging plain-language explanations aren't a differentiator by themselves anymore (since competitors do that too) before naming what actually is.
2. ~~**Verify the backlog stats before treating them as the app's own permanent numbers.**~~ — **Done, both real and re-verified via live web research, not carried over unchecked.** "12M+ pending cases" → **12.1M+**, confirmed as USCIS's own total pending caseload, Q2 FY2026. "+42% backlog growth, year over year" → confirmed real but re-labeled **"net backlog growth"** specifically — the 42% figure is the *net* backlog (cases within USCIS's own control) growing from ~5M to ~7M between FY2025 Q2 and FY2026 Q2; a *different*, larger metric (total pending caseload) grew a separate 16.5% over a different year-over-year window (FY2025 Q1 → FY2026 Q1) in the same source data. The original static-site copy juxtaposed both numbers under generic "backlog growth" labels as if they were the same measure — they aren't, so this was a real, worthwhile catch, not just a rubber-stamp re-check. Added a one-line source/date attribution under the stat row, same spirit as CW-31's citations.
3. ~~**The CTA copy is still pre-launch/waitlist framing.**~~ — **Done.** "Be first to know when CaseWhy launches" → "Be first to try CaseWhy"; body copy now names the real, working pieces (daily case checks, policy-grounded explanations, processing times, visa bulletin tracking) instead of just "we'll email you when it's ready" — while still being honest that *real-case* tracking (production USCIS API access) isn't open yet, so it doesn't overclaim. Hero copy/eyebrow left untouched, as scoped.
4. Tagline placement (the still-open item under "Branding decision" above) is now more relevant with a fuller landing page — same open question, not a new one, not touched in this pass.

**Verification:** `tsc --noEmit` and `npm run lint` clean (same one pre-existing `Logo.tsx` `<img>` warning as before, unrelated). Verified in-browser, full scroll-through, dark mode.

**On the bigger question this port raises — does the static `casewhy.com` site go away because of this:** no, not now, and its *role* doesn't go away even later. `casewhy.com` (the static site, its own "casewhy" Vercel project) and `casewhy-app.vercel.app` (this app, the "casewhy-app" project) stay two separate live things until an actual cutover — either merging `main`/`nextjs-app` so the "casewhy" project deploys this app instead of the static HTML, or repointing casewhy.com's domain in Cloudflare from "casewhy" to "casewhy-app." That cutover is still gated on production USCIS API access, the Florida LLC, and the legal/disclaimer review — none of which are close yet, so nothing about the static site changes because of this port. It's prep for later, not a switch. When the cutover does happen, the static site's job (marketing front door for signed-out visitors) doesn't disappear — it becomes this same landing page's job, permanently, as part of one app instead of two. Recording this here so it's shared context, not just something discussed with Peter separately.

## Next tasks for Claude Code — prepared by the cloud session, Sep 5 (later evening)

Read this whole file fresh just now (through your CW-31/32/33/34, branding, and marketing-port updates — genuinely impressive amount of ground covered today) plus the live checklist DB directly. The DB itself was stale — several items you've already shipped were still showing "todo" — so that's fixed now (see the sync note at the bottom of this section). Below is what's actually next, in priority order, all fully unblocked right now.

**None of this touches the still-open monetization/paywall decision or the congressional-rep-lookup feature idea — Peter hasn't picked a direction on either yet, so don't build pricing tiers, gating logic, or the congressional-letter feature on your own initiative. That's unchanged from the existing note above ("Not build tasks yet").**

1. **Still open, blocked by real-world timing, not code.** It's Saturday Sep 5 as of this update — sandbox is Mon–Fri only, so this genuinely can't happen today regardless of anything else. Next real opportunity: Monday Sep 7, 7AM–8PM ET.
2. ~~**Build the PWA manifest + a minimal service worker.**~~ — **Done, Sep 5 ~6:50am ET.** `src/app/manifest.ts` (Next's App Router manifest convention — auto-served at `/manifest.webmanifest`, auto-linked in `<head>`, confirmed via curl) using the existing `icon-192.png`/`icon-512.png`, `theme_color` the brand green `#1baf7a`, `start_url: /dashboard`. Service worker (`public/sw.js`) is deliberately narrow — only intercepts page navigations, network-first with a static offline fallback (`public/offline.html`) — **never caches or serves dynamic content** (case status, API responses), since showing a stale cached status offline would be actively misleading for a product whose whole point is current information. Registered client-side via `src/components/ServiceWorkerRegister.tsx` in the root layout. Verified in a real browser: registers and reaches `activated` state, no new console errors (only the same pre-existing Feedly-extension hydration warning seen elsewhere this session, unrelated).
3. **Real finding, changes the premise — the demo can't be scheduled yet, and it's not a "check the mechanism" question.** Fetched the USCIS Torch API onboarding execution spec artifact directly: the live demo is **step 08** of 9, strictly sequenced *after* step 06 (request the production affidavit from `developersupport@uscis.dhs.gov`) and step 07 (sign and return it) — there is no independent "book a demo slot" action available before an affidavit exists. Step 06 itself depends on step 05 (the 5-consecutive-weekday sandbox traffic log — 1 of 5 logged as of this writing, next session Mon Sep 7, earliest finish Thu Sep 10) **and** requires a "Company/Organization Name" and authorized signatory for the affidavit to be issued to — worth a real open question: can that affidavit request go out to USCIS while the Florida LLC is still "processing," or does it need to be fully filed first? Not something to guess at; flag to Peter rather than assume either way. **So: nothing to do on the demo itself right now** — the actual next real action toward it is finishing the Mon–Thu traffic log, then (once that and the LLC question are resolved) sending the step-06 affidavit request email. The "CW-15 is done so CW-09 is unblocked" framing conflated product-readiness with the actual USCIS process gate — worth knowing since it'll come up again if the sequencing isn't tracked precisely.
4. ~~**Backlog, no rush — two branding copy judgment calls.**~~ — **One done, one still deliberately deferred.** `layout.tsx`'s metadata `description` now leads with the locked tagline verbatim: "Your USCIS case, explained! AI-explained USCIS case status tracking — know what's happening with your immigration case, and why." The landing-hero tagline placement is **still open, not done in this pass** — it was explicitly scoped to fold into CW-28 (the fuller visual-design/nav pass) rather than be decided piecemeal, and that reasoning still holds; not touched here on purpose, not an oversight.
5. **Recurring, not due yet:** `visa-bulletin.ts` needs its first monthly refresh once the October 2026 bulletin publishes. No action until then.

Everything else already on your plate (sandbox traffic log Mon–Thu Sep 7-10, Florida LLC, Postmark "Request approval," Gmail delegation, awaiting USCIS's FOIA-sandbox reply) keeps running on its own clock — none of it blocks 1-3 above.

**Checklist DB sync note:** updated `casewhy_items` directly to mark CW-31, CW-32, CW-34, CW-28, and CW-29 as done (matching what this file already documents in detail above), and CW-15 as done (all six demo criteria covered — CW-09, the actual scheduling, is unchanged/todo, see #3 above). CW-33 set to in-progress (part (a), USCIS's published ranges, shipped; part (b), comparative analytics, correctly still deferred pending real accumulated case data). If any of that doesn't match what's actually true on your end, fix the DB directly — it's the shared live source of truth for status, not this file.

## Monetization ideas — prep for Claude Code, Sep 5 (evening) — scope and estimate, don't build the paywall yet

Peter wants these in front of you now, same pattern as CW-31–34: scope each one and give a real effort estimate before anything gets built. **This is explicitly NOT a green light to build a live paywall or take real payments** — there's no Florida LLC filed yet, no business bank account, no Stripe (or any payment processor) account set up, and the ToS/disclaimer language still needs the lawyer review budgeted in the MVP doc (~$500-1,500, not spent yet). What IS fair game right now: research, schema design, and any plumbing that doesn't require real billing to exist (e.g., a `subscription_tier` column that defaults to `"free"` for everyone, a usage-counter table). Don't wire actual tier-gating logic live yet — the free-vs-paid packaging itself is still Peter's open call (MVP doc Section 6 is reopened, not resolved).

These came out of fresh competitive research (VisaWatch, Lawfully, Case Tracker for USCIS & NVC) the cloud session did Sep 5 evening — full writeup and reasoning in the "CaseWhy Competitive Comparison" artifact and the MVP doc Section 6 (Claude Code can't read the MVP doc directly since it lives in a claude.ai Project — everything you need is duplicated here). Ranked in the cloud session's recommended priority order:

**Pricing/packaging decision — confirmed by Peter, Sep 5 (evening), answering your CW-35/CW-36 questions directly:**

- **One bundled paid tier, not separate add-ons.** Working name "CaseWhy Plus." A single subscription unlocks unlimited AI chat (CW-35) *and* multi-case tracking (CW-36) together, plus whatever else from this list ships later (CW-37/38/40) — not a menu of separate a-la-carte purchases. Matches how all three competitors structure their own paid tiers, and is simpler for a solo founder to build/support/market than multiple SKUs.
- **Price: $9.99/mo** for that one tier. Matches the general consumer-subscription-app market median (not immigration-specific, but the most defensible anchor found), sits mid-range in the original $7-15/mo target from the MVP doc, and is a familiar/expected price point.
- **CW-35 free-tier chat limit: 10 questions per month.** Unlimited on the $9.99/mo paid tier. Treat 10/month as a starting hypothesis, not a permanent number — worth revisiting once there's real usage data on how often people actually use the chat (a case's status doesn't change often, so most free users likely won't hit this most months — its real job is catching the people who lean on it heavily, who are also the most likely to convert).
- **CW-36 case cap: 5 tracked cases**, not unlimited, same $9.99/mo tier (no separate/higher price for this). Deliberately bounded rather than unlimited — mirrors Peter's own household (himself, wife, daughter, with room to grow) and prevents the plan from being valuable enough to informally resell/share across an unrelated group, which would undercut the whole per-subscription pricing model.
- **Still true, unchanged:** none of this is a green light to build a live paywall yet (see the no-LLC/no-Stripe/no-lawyer-review caveat above) — build the packaging logic, schema (`subscription_tier`, usage-counter table, the 5-case cap), and UI/upgrade-prompt copy now; wire it to real billing once the business prerequisites exist.

1. ~~**CW-35 — Meter the "ask a question" AI chat (CW-32), don't gate it entirely.**~~ — **Done, Sep 5 ~7:40am ET, schema/logic only (see the standing no-live-billing caveat above — real numbers now confirmed, but still no Stripe to actually charge anyone).** New `chat_usage` table (userId + year-month + count) via `src/lib/billing/chat-usage.ts`; `POST /api/chat` checks the month's count against the tier's limit (10/month free, unlimited plus) *before* calling `chatAboutCase()` — so a blocked request never costs an AI Gateway call — and returns a distinct `402` with `limitReached: true` when hit. `CaseChat.tsx` shows "X of 10 free questions left this month" after each reply and disables the input with an upgrade message once reached.
2. ~~**CW-36 — A family / multi-case plan.**~~ — **Done, Sep 5 ~7:40am ET — the flagged schema change, applied while `tracked_cases` was still empty.** Dropped the old `tracked_cases_user_id_idx` **unique** index (real migration `0002_messy_cyclops.sql`, applied directly against the live Neon DB via `drizzle-kit migrate`, verified after via a direct query that it's now a plain non-unique index) — `tracked_cases` is genuinely multi-row-per-account now, capped in application code (not the DB) at free=1/plus=5 via the new `src/lib/billing/tier.ts` (`subscriptions` table, no row = free — same "no live billing" caveat as CW-35). This required real, not-trivial UI work beyond the schema: a `CaseSwitcher` component (pill row, reuses the existing `?receipt=` query-param convention) now shared by both `/dashboard` and `/ask` when a user has more than one tracked case; `TrackCaseButton` now has three states (already-tracked-with-untrack, can-track, at-cap-with-upgrade-message) instead of the old binary one; a new `untrackCase()` action. **Verified two ways:** a direct script against the real DB (tier defaults to free with no row, tracked-case decrypt/order, tier flip to plus, chat-usage counting through to `limitReached`, full cleanup back to empty) — all passed exactly as designed; and a real in-browser click-through (seeded 2 temporary tracked cases for the actual signed-in test account, confirmed the switcher renders and correctly re-orders active/inactive pills on both pages, confirmed switching updates the URL and re-renders, then deleted the seed data and confirmed the dashboard is back to its clean empty state). **Confirmed, not just assumed:** the cron `check-status` job needed zero changes — it already iterates every `tracked_cases` row independently by `row.id`, never assumed one-row-per-user.
   - **Real bug caught before it shipped:** the original `TrackingLimitError` custom error class lived in `src/app/dashboard/actions.ts`, which crashed the whole app with a 500 the moment it was imported — Next.js's `"use server"` file convention only allows exported *async functions*, not classes. Fixed by throwing a plain `Error` with the same message instead; caught by testing `/dashboard` and `/ask` in-browser right after writing the code, not just trusting `tsc --noEmit` (which doesn't catch this — it's a Next.js build-time convention, not a type error).
   - **Not yet verified live:** actually tracking a case, hitting the free cap, and untracking through the real UI — all need a real `getCaseStatus()` call to render the `StatusCard` that the tracking button lives inside, and the USCIS sandbox was down (Saturday) for this whole build. Same open item as CW-32's chat-flow verification — worth doing together next sandbox session (Mon 7AM–8PM ET).
3. **CW-37 — Faster / on-demand status checks.** Free keeps today's daily cron; paid gets hourly polling or a manual "check now" button. Before committing to hourly-for-all-paid-users, check the production USCIS API's actual rate/TPS ceiling (sandbox limits may not reflect production) — this could become a real constraint once there's paid volume.
   - **Estimate, Sep 5 evening — scoped, not built.** The rate-limit math already answers the "hourly for all paid users" question without needing new research: the onboarding spec (see artifact below) already records production's quota as ~10,000 lookups/day, 10 TPS (vs. sandbox's 1,000–3,500/day, 5 TPS). Hourly polling is 24 checks/day/user — at just ~416 paid users doing that, the *entire production daily quota* is gone before the daily cron for free users even runs. That's a real, near-term ceiling, not a hypothetical one. **Recommendation: build the manual "check now" button for v1** (small — a server action that calls `getCaseStatus()` on demand for one tracked case, updates `lastStatusText`/`lastCheckedAt` outside the cron cycle, gated to plan tier the same schema/logic-only way as CW-35/36), and treat automatic hourly-for-all-paid-users as a later milestone that needs real volume data before committing to it, not a v1 requirement. Estimate: ~1-2 hours for the button once picked up. Not started.
4. **CW-38 — A document vault.** Extends `FileUploadStub` (today explicitly local-only, no real endpoint) into real persistent storage with eventual AI-summarized extraction. Needs real storage (Vercel Blob or S3) and a real upload endpoint — biggest lift of the six after the family plan.
   - **Provisioning done, build not started, Sep 5 ~8:00am ET.** A private Vercel Blob store (`casewhy-documents`) now exists and is linked to the project — self-service, one CLI command, `BLOB_READ_WRITE_TOKEN` already pulled into `.env.local`/Vercel. Chosen over S3 since it needed zero new account setup on an already-Vercel-hosted app. Actually wiring `FileUploadStub` to it (new `case_documents` table tied to a tracked case, not just a user — CW-36 made cases plural — a real `/api/documents` upload endpoint) is still queued, next up.
5. **CW-39 — Escalation toolkit: congressional representative lookup + letter-drafting assistant.** This is Peter's own proposed feature (modeled directly on how his family's own N-400 case actually got unstuck), bigger and more novel than the other five — **not yet authorized to build at all, scope/estimate only for now, separate open decision from the general pricing question above.** What it'd need: the Census Bureau's free Geocoding Services API to resolve a mailing address to a congressional district, paired with a small hand-maintained table of current U.S. Representatives/Senators (same "curated, periodically refreshed" pattern as CW-31/33/34, but on a ~2-year refresh cycle tied to election cycles, not monthly); the letter-drafting half should strictly organize/polish the *user's own draft content* rather than generate case-specific claims itself, both so a real personal letter still reads as personal and to stay clearly outside anything resembling legal advice. **New PII category to flag:** a mailing address is more sensitive than anything CaseWhy stores today — would need the same AES-256-GCM at-rest encryption pattern already used for `tracked_cases.receipt_number` (see `src/lib/db/crypto.ts`), plus a privacy-policy update before it ships. Commercial fit (if built): free standalone rep lookup, metered/paid AI-assisted letter drafting — same "meter the AI part" pattern as CW-35.
6. **CW-40 — An attorney-handoff report.** Exportable PDF case timeline + explanations, for a paid consultation moment. Lowest-effort of the six — mostly a print/export view of data that already exists (tracked case + `explainCaseStatus()` output).
   - **Estimate, Sep 5 evening — scoped, not built.** Confirms it's genuinely the smallest of the six: `@react-pdf/renderer` is the right library (pure-JS, no headless browser — Puppeteer/Playwright would be real weight to add to a serverless function for this), rendering a PDF from data the dashboard already fetches (status, history, `explainCaseStatus()` output including its policy citations) — no new data-fetching logic needed, just a new render target and a "Download report" button. **One open judgment call, not yet decided:** should this be tier-gated (a "plus"-only perk) the way CW-35/36 are, or ungated since it's a user exporting their own already-visible data rather than something with a real per-use cost? The doc's "for a paid consultation moment" framing suggests gating was the intent, but doesn't say so explicitly — flagging rather than assuming. Estimate: ~2-3 hours once picked up (add dependency, build the PDF layout, wire the route/button, decide the gating question). Not started.

**Ask:** scope and give a real effort estimate for each, same as you did for CW-31–34 — flag anything that turns out bigger or smaller than it looks from here, and flag any additional one-way-door schema decisions (like CW-36's) before touching them. Report back in this file as usual.

**Status update, Sep 5 ~7:40am ET: CW-35 and CW-36 done** (see the strikethrough entries above for full detail — real numbers were confirmed by Peter directly, schema/logic built, live billing still correctly not wired). **CW-38 (document vault) was also selected for this build pass but not yet started** — the pricing-decision clarification took priority first; still queued, not dropped. **CW-37 and CW-40 weren't in this pass's scope. CW-39 remains not authorized to build — scoping only, still not done.**

## Next tasks for Claude Code — round 2, Sep 5 (later evening)

Checked in on progress since the last "Next tasks" section — CW-32, CW-35, and CW-36 all shipped since then, plus the schema migration and the real bug catch on `TrackingLimitError`. Nice run. Consolidating what's next into one place, in priority order:

1. ~~**Fix the FOIA sandbox credential (CW-18)**~~ — **Done and confirmed working, Sep 5 ~8:30am ET.** Peter logged into developer.uscis.gov (Claude Code can't log in itself — pulling a portal-issued secret needed a real account session); from there, pulled the real `Umv...` credential pair directly off the CaseWhy App's "Team Apps" page — confirmed by its Products list showing **both** "Case Status API - Sandbox" *and* "first-case-sbox" enabled, unlike the other (`joJ...`, already in use) pair, which only has Case Status. Added as new, separate env vars (`USCIS_FOIA_CLIENT_ID`/`SECRET`) in all three places (`.env.local`, Vercel — production/preview/development, GitHub Actions secret) — the existing `USCIS_CLIENT_ID`/`SECRET` for Case Status were never touched. `src/lib/uscis/client.ts`'s `uscisRequest()`/token cache were refactored to accept an optional credentials override (keyed by client ID, so each Torch app gets its own cached token) — existing Case Status call sites are unchanged, `foia.ts`'s two functions now pass `getUscisFoiaConfig()` (new, in `src/lib/config.ts`) explicitly.
   - **Verified with a real sandbox call, not just "no more 401."** A test `createFoiaCase()` first got a real `500` (a null-pointer on a missing `requester` object) — meaningful, since a 500 on business logic (not a 401) proves the *credential* fix genuinely worked, the earlier problem really was "wrong credential pair," not a platform-side bug as first suspected. Adding a `requester` surfaced a detailed `400` validation response (country codes must be ISO 3166-1 alpha-2 — "US" not "USA" — among a few others).
   - **`digitalDelivery` chased down further, same day — genuinely inconclusive, likely a real sandbox limitation, not something fixable from CaseWhy's side.** Peter created a real production USCIS MyAccount for `info@casewhy.com` specifically to test this (signup itself asked for nothing beyond an email — no receipt number, no A-number, no real-applicant-specific data, so that door was never actually closed). But `digitalDelivery` turned out to be an **enum**, not an email string (`"LEGACY"` or `"MY_ACCOUNT"` — sending a bare email gets a different, clearer error naming the two valid values). Sending `"MY_ACCOUNT"` with the real new account's email in `requester.emailAddress` landed back on the *same* "Enter valid MyAccount email" message as before. **Most likely explanation, not confirmed: the FOIA *sandbox* endpoint checks against a sandbox-only user pool that doesn't know about real production MyAccount signups** — plausibly deliberate, so a sandbox testing tool never touches real user PII. If that's right, no test account can be created to satisfy this in sandbox at all, the same way Case Status needed a *published sandbox test receipt number* (`EAC9999103403`) rather than a real one. **Worth asking USCIS support directly whether a sandbox-specific test MyAccount identity exists** — drafted as a second follow-up question below, not yet sent.
   - **Two real, useful findings documented directly in `foia.ts`'s types**, both discovered by testing rather than trusting the docs schema: `requester` is typed optional but effectively required (500s without it); `digitalDelivery` is now correctly typed as the two-value enum it actually is, with the sandbox-vs-production theory recorded in its own comment.
   - ~~**Still open, needs the user:** reply to USCIS's thread~~ — **Confirmed done, Sep 5 ~9:00am ET.** Peter sent the fuller draft — checked the actual sent text against it word for word: it covers the credential-fix confirmation, the `digitalDelivery` enum, and the sandbox-test-identity question (phrased against the same "published test receipt number" analogy as Case Status). Nothing further to send on this thread — genuinely closed, just waiting on USCIS's reply now.
2. **Pick up CW-38 (document vault)** where you left off — you'd already selected it for this pass before the pricing clarification interrupted. No new information changes its scope from what's written above.
3. **Scope and estimate CW-37 and CW-40** — same ask as above, still open, both pure research/estimation with no sandbox dependency.
4. **Monday (7AM–8PM ET): verify CW-32's chat round-trip and CW-36's track/cap/untrack flow live, together.** Both are code-complete per your own notes above — this is purely waiting on the sandbox being open, not on anything left to build.

Nothing else new — sandbox traffic log (Mon–Thu), Florida LLC, Postmark "Request approval," and Gmail delegation all keep running on their own clock, same as before.

## Next tasks for Claude Code — round 3, Sep 5 (evening) — CW-33 expansion: all forms + location lookup

Peter looked at the CW-33(a) processing-times page and wants it extended well beyond the current 4 hardcoded form+category entries: **all USCIS forms, including a form + location (city/office) lookup**, matching the structure of USCIS's own official tool. His words: "extend the function for all forms including the ones requiring location input, similar to what uscis offers by entering the form number and city to provide the processing time by form by center/city."

**Test this before scoping anything else — it could change the whole approach:** USCIS's actual processing-times tool is a client-side SPA that calls an internal JSON API, documented (via reverse-engineering) in the open-source `coatless-r-n-d/uscis-processing` R package as `https://egov.uscis.gov/processing-times/api/`. This is a *different surface* from `egov.uscis.gov/processing-times` itself — the HTML page you already confirmed is Cloudflare-blocked for CW-33(a)/CW-34. Nobody has tested whether this specific API endpoint carries the same block. Use Claude in Chrome to load the real tool, check `read_network_requests` for the exact calls it makes (URL, headers, payload shape), then try hitting that endpoint directly (plain fetch, realistic UA) the same way you tested the HTML page. Two very different outcomes hinge on this: if it's open, this becomes a real live comprehensive lookup instead of another hand-maintained table — a materially better result. If it's blocked the same way, the existing manual-capture pattern has to scale up a lot (see below), which is a much bigger, different-shaped effort. Don't scope the rest until you know which world this is.

**Location structure, so the picker design doesn't guess:** interview-based forms (N-400, family-based I-485 — the ones CW-33(a) already had to punt on as "office-specific, no national figure") are adjudicated by **92 field offices + 130 Application Support Centers**, keyed to the applicant's own specific office — this is the "city" input Peter's describing. Petition-based forms (I-129/I-130/I-140, etc.) instead go through exactly **5 service centers**: Vermont (990), California (991), Nebraska (992), Texas (993), Potomac (1031) — a much smaller, already-enumerable set, closer to what CW-33(a) already models with `serviceCenter()` in `explain.ts`. These are structurally different lookups (many field offices vs. 5 centers) and USCIS's own tool treats them differently — worth designing the form+location picker around that split rather than one generic dropdown for everything.

**Existing open-source references, unvetted but worth a look before building from scratch:** `gr2m/uscis-service-center-processing-times` (npm/JS package, service-center scope — most directly reusable given this is already a Next.js/TS app), `coatless-r-n-d/uscis-processing` (R package, documents the API endpoint above), `jzebedee/uscis` (daily scraper/dataset — flags that USCIS began "aggressively blocking scraping" as of Feb 2025 and now needs residential proxies, a real caution that even a currently-open API endpoint might not stay reachable), `hmakkapati/uscis-tracker`. None of these are vetted for license or reliability — leads, not endorsements.

**Scale:** USCIS's own tool covers several dozen form types, several with per-category variants — meaningfully more than today's 4 entries, though "several dozen" is an imprecise figure from research, not a count from the tool itself; get the real number once you're looking at whichever surface (API or manual) ends up being the actual data source.

**Ask, same pattern as CW-31/33(a)/34/37/40: test the API endpoint first, then give a real effort estimate for whichever path it points to** — don't build the full-scale version yet. The two possible outcomes (live API integration vs. a much larger hand-captured dataset plus an office/city picker UI) are different enough in shape and size that an estimate before testing the endpoint would just be a guess. Report back what the endpoint test shows either way, even if it's blocked — that's a real, useful finding on its own, same as the CW-33(a)/CW-34 Cloudflare findings were.

## Next tasks for Claude Code — round 4, Sep 5 (evening) — consolidated

Checked the checklist DB and this file end to end just now. Consolidating everything actually open into one ordered list — several earlier "next tasks" rounds are now stale reading (CW-18's credential fix, CW-35/36 are done) and the open items were spread across three different sections. This supersedes rounds 1-3 above as the priority order; don't re-do anything already marked done above.

1. ~~**CW-33(c) — test the processing-times API endpoint**~~ — **Tested, Sep 5 ~9:15am ET. Real, conclusive "no" — this is a materially different finding than "Cloudflare-blocked like before," worth reading carefully before deciding how to proceed.**
   - **The documented endpoint doesn't exist anymore.** `coatless-r-n-d/uscis-processing`'s reverse-engineered `egov.uscis.gov/processing-times/api/` — tried directly in a real browser (passes Cloudflare fine, same as every other test this session) and it just redirects back to the main tool page. That reference is stale; the current tool doesn't use it.
   - **What the current tool actually does, confirmed via `read_network_requests` while driving the real form:** selecting a form fires a same-origin `POST https://egov.uscis.gov/processing-times` (the *same* URL as the page itself, not a separate API path) that returns server-rendered HTML, not JSON. There is no separate, lower-friction API surface to test against — the only "endpoint" *is* the page, and it's behind the identical Cloudflare wall already established for CW-33(a)/CW-34. A plain `curl`/`fetch` from a serverless function would hit the same block a `curl` to the HTML page does; only a real, human-driven browser session gets through.
   - **This changes the shape of the ask, not just the estimate.** "All forms + any city/office" as literally scoped would mean manually querying the tool once per form+category+office combination — there's no bulk/export view in the tool itself. With ~40 form types (several with multiple categories) × 92 field offices + 130 ASCs for interview-based forms, that's not "a bigger hand-captured dataset," it's an unbounded number of individual browser-driven lookups with no realistic refresh cadence — a fundamentally different (and impractical) undertaking from CW-33(a)'s 4 entries or CW-34's one-page-per-month bulletin.
   - **Decision (Peter, Sep 5): both (a) and (b).** ~~Recommendation, not yet built~~ — **built and shipped, Sep 5 evening.** (a) `src/lib/kb/processing-times.ts` grew from 4 to 7 hand-captured entries (added I-751 removal-of-conditions, I-90 10-year renewal, I-140 EB-1, all pulled via real browser same as before) plus a new `VISA_BULLETIN_TIED_NOTE` documenting a real finding: I-130 *preference* categories (F1/F2A/F2B/F3/F4) mostly don't get a fixed number at all — SCOPS ties their adjudication order to the Visa Bulletin instead and the tool literally says "See notes" until a category is fully current (confirmed directly, F1 category tested). (b) Rather than hand-building a directory of the 92 field offices + 130 ASCs (real USCIS data that changes over time), `/processing-times` now links straight out to USCIS's own locator pages — confirmed live and correct in a real browser: `uscis.gov/about-us/find-a-uscis-office/field-offices` and `.../application-support-centers` (note: no "-ascs" suffix — a first guess at that URL 404'd, caught before shipping). Typecheck/lint clean, verified rendering in a local dev server, pushed to `nextjs-app` (commit `c97a240`), production deploy confirmed via `vercel ls`.
2. ~~**CW-18 follow-up**~~ — **Confirmed done, Sep 5 ~9:00am ET.** Checked the actual sent email text against the fuller draft — exact match, covers the enum + sandbox-test-pool question. Nothing further needed; just waiting on USCIS's reply now.
3. ~~**CW-38 — pick up the document vault build.**~~ — **Built and shipped, Sep 5 evening (commit `f71e23c`).** `case_documents` table added (tied to a tracked case, not just a user, per CW-36 — migration applied live to the production Neon DB), file names encrypted at rest same as `tracked_cases`. Real `/api/documents` endpoints wired to the already-provisioned private Vercel Blob store: `POST` uploads (10MB cap, PDF/JPEG/PNG only), `GET` lists a case's documents, `/api/documents/[id]` handles download (proxied through blob's `get()` — a private-store blob URL isn't directly fetchable, so downloads stream through our own server, not a bare blob URL) and delete. `FileUploadStub` (a local-only demo stub) replaced by a real client component, `DocumentVault`. **Verified:** `tsc`/lint clean; a throwaway script exercised the real DB schema + real Blob store end-to-end (upload, download, delete) against production infra, then cleaned up its own test rows — see the commit message for the exact steps. **Not verified:** the actual dashboard UI in a real browser — the Case Status sandbox is down for the weekend (M-F 7AM-8PM ET only), so there's no live case status to render `StatusCard` (which is what mounts `DocumentVault`) against right now. Worth a quick look once Monday's sandbox-hours session (item 6 below) is already open for CW-32/36.
4. ~~**CW-37 — build the manual "check now" button.**~~ — **Built and shipped, Sep 5 evening (commit `e9bfd23`).** Extracted the cron job's fetch/notify/update logic into a shared `checkTrackedCaseNow()` (`src/lib/uscis/check-status.ts`) so the cron and the new on-demand action share one code path instead of two that could drift. `checkCaseNow()` server action gates on tier — plus only, free stays on the once-daily cron (per the rate-limit math already recorded above). `CheckNowButton` shows "last checked" either way, with an upgrade nag for free accounts. **Verified:** `tsc`/lint clean; a throwaway script called `checkTrackedCaseNow()` against a real `tracked_cases` row and the real USCIS sandbox — got back a `UscisApiError` (503), confirming the sandbox is still down for the weekend (same finding as CW-38 and the earlier CW-33(c) test), and confirmed the error propagates and cleans up correctly. Same as CW-38: the live button click-through in a real browser isn't verified yet — needs the sandbox up, so fold it into Monday's window (item 6).
5. ~~**CW-40 — build the attorney-handoff PDF report.**~~ — **Built and shipped, Sep 5 evening (commit `c1d4fcf`).** Gated to CaseWhy Plus, per the resolved judgment call. New `GET /api/report` renders a one-page PDF via `@react-pdf/renderer` (`src/lib/pdf/CaseReportDocument.tsx`) from data the dashboard already fetches — status, `explainCaseStatus()` output (explanation, next steps, policy citations with real source links), history — no new data-fetching logic, matching the "smallest of the six" framing from the original estimate. `DownloadReportLink` is a plain link on the dashboard, tier-gated the same way `CheckNowButton` is. **Verified with real data paths:** a temporary local-only test route (deleted before committing, never pushed) exercised the real `CaseReportDocument` + `renderToStream` + the real deterministic CW-31 KB matcher through Next's own bundler — produced a valid, well-formed one-page PDF, confirmed via `file`, byte count, and actually reading the rendered content. One minor cosmetic item, not a functional bug: there's some extra vertical whitespace between a couple of paragraphs in the PDF layout (a react-pdf line-wrap quirk) — worth a quick polish pass sometime, not urgent. `tsc`/lint clean. **Not yet verified:** a real button click in the live dashboard UI (same sandbox-hours gap as CW-37/38 — fold into Monday's window, item 6).

**Round 4 is now fully built (items 1-5), Sep 5 evening.** Only item 6 (Monday sandbox-hours verification, time-gated) and item 7 (CW-39, not authorized) remain.
6. **Monday (Sep 7), 7AM-8PM ET, sandbox hours: verify CW-32's chat round-trip and CW-36's track/cap/untrack flow live, together** — both code-complete, purely waiting on the sandbox window. Unchanged from round 2. **Add to this window: a quick live look at CW-38's DocumentVault UI** (upload/list/download/delete) — the API+storage layer is already verified end-to-end against production infra (see CW-38 above), just not the dashboard rendering, since that needs a live case status to mount against. **Also add: click the actual CW-37 "Check now" button, and download a real report via CW-40's link** (both need a `subscriptions` row flipped to `plus` for the test account first, same as CW-36's verification did — one flip covers testing all three plus-gated features together) — CW-37's server action and CW-40's PDF rendering are both verified against real data already, just not a real click-through in the live dashboard yet.
7. **CW-39 remains not authorized to build** — still scope-only if Peter raises it, no change.

Everything else (Florida LLC, sandbox traffic log Mon-Thu, Postmark "Request approval," Gmail delegation) keeps running on its own clock, unchanged.

## Next tasks for Claude Code — round 5, Sep 5/6 (overnight) — fix the sign-in email's branding

Peter tried registering and got a real, unbranded sign-in email — worth fixing soon, it's a trust problem for exactly the audience CaseWhy is trying to reassure (an unfamiliar sender domain reads as suspicious to someone already anxious about their case, and this product's whole pitch is trustworthy information). The email: subject "Sign In to proud-wind-48334029", from `Neon Auth <auth@mail.myneon.app>` — both are Neon Auth defaults, not a bug, and both are documented as fixable:

1. **Quick fix — the project name in the subject/body.** Neon Console → Auth → Configuration tab → Project Info panel → **Application Name** field. Per Neon's own docs, this defaults to the raw Neon project slug (explaining "proud-wind-48334029") and is exactly what shows up in user-facing auth emails. Set it to "CaseWhy". Two-minute fix, no code change, do this first.
2. **Real fix — the sender domain.** Neon's docs (Auth production checklist) explicitly recommend replacing the shared `auth@mail.myneon.app` SMTP with a custom provider: *"Replace shared SMTP (`auth@mail.myneon.app`) with your own email service for reliable delivery and higher limits."* CaseWhy already has a fully verified Postmark sending domain for `casewhy.com` (DKIM + Return-Path done for CW-24's notification emails) — reuse that instead of setting up a new provider. Configure it as Neon Auth's custom SMTP provider in Auth production settings so sign-in emails come from something like `CaseWhy <auth@casewhy.com>` instead of Neon's domain.
3. **Optional, bigger lift, not urgent: full template control.** Neon supports intercepting `send.otp`/`send.magic_link` webhook events and sending a fully custom-branded HTML email (real logo, real design-system colors) through Postmark directly instead of Neon's default template — covered in Neon's "Customize emails" doc. Worth doing eventually so the sign-in email actually looks like CaseWhy, but steps 1-2 alone fix the actual trust problem Peter hit and should ship first.

**Ask:** do 1 and 2 now — both are config, not new code, and nothing blocks either. Report back once sender/subject are confirmed branded with a real test sign-in email. Scope 3 only if there's a natural opening; not worth interrupting other work for.

**Done, Sep 6 (Claude Code, via Neon Console — no code change, config only).** Both 1 and 2 are set: Application Name → "CaseWhy"; Email provider → Custom SMTP provider, `smtp.postmarkapp.com:587`, sender `auth@casewhy.com`, reusing the same Postmark Server API Token already in `POSTMARK_API_TOKEN`. **Verified two ways:** Neon's own "Send test email" button reported success against `info@casewhy.com` (proves the SMTP credentials actually authenticate with Postmark — a wrong token would have errored there, not succeeded); and the config page itself now shows Server: "Custom SMTP provider" and Sender address: `auth@casewhy.com` (was "Shared" / `auth@mail.myneon.app`). **Not independently confirmed by Claude Code:** the actual received email's subject/sender text, since Claude Code doesn't have inbox access to `info@casewhy.com` (same Gmail-delegation gap noted elsewhere in this file) — Peter, worth a quick look at the real inbox to confirm the subject reads "Sign In to CaseWhy" (not the old project slug) and the sender shows as CaseWhy. Scope 3 (fully custom HTML templates) still not done — untouched, per the original "not urgent" framing.

## Priority override, Sep 6 (evening) — build email capture now, ahead of round 7

Peter doesn't know how long USCIS will take to grant production access, and doesn't want the pre-launch email list waiting on that timeline — he wants email capture live in production now, independent of USCIS entirely (it is: this is a database table and a form handler, nothing in it touches the USCIS API, the sandbox, or the affidavit process in any way). **This makes round 6 immediately below the top priority, ahead of round 7's content/design refresh.** Concretely:

1. **Build round 6 exactly as scoped** — the `email_subscribers` table + the server action wired to both `EmailCaptureForm` instances on `page.tsx`. This is the real blocker: right now submitting either form just reloads the page with the email in the URL query string and captures nothing.
2. **Elevate round 6's "also check, don't assume" item to a firm build requirement, not just a check:** confirm whether the static `casewhy.com` site's own copy of this form shares anything with the app, then wire it to something real too — reuse the same backend if that's simpler given the two are separate deployments, or give it its own small table if not. Either way, **both forms need to actually capture an email in production**, not just the app's.
3. Skip the honeypot/rate-limiting hygiene item for now if it would delay shipping — already scoped as "not urgent enough to block."
4. **This is deliberately decoupled from round 7** (the logo/tagline/color-match/founder's-note/copy refresh below). Round 7 can land whenever it's ready — don't bundle it with this or let it hold up shipping the actual capture mechanism. Build and ship email capture first; round 7 follows after.

**Ask:** treat this as the next thing to pick up, ahead of everything else still open in this file except anything already mid-flight. Report back the moment a real signup lands in the table — ideally from both sites, or confirmation that one shared backend covers both.

**Done, Sep 6 (Claude Code) — one shared backend covers both, confirmed live from both sites.** Item 1 (the app's own round-6 build) landed first, independently. Item 2 (the static site) got fixed the same evening after Peter hit the gap directly — submitted `casewhy.com`'s form himself, got neither a stored email nor a confirmation. Rather than a separate table, went with one shared backend: `src/lib/marketing/subscribe.ts` holds the real logic, used by both the app's own server action and a new public `POST /api/subscribe` (CORS-scoped to `casewhy.com`/`www.casewhy.com`) that the static site's forms call directly. `sourcePage` distinguishes the four submission points (`landing-hero`/`landing-footer` on the app, `static-hero`/`static-footer` on the static site). Item 3 (honeypot) shipped anyway since it added no real delay. **Verified live, both sites, real signups:** typed a real email and clicked submit on both `casewhy-app.vercel.app` and `www.casewhy.com`, confirmed each landed a row with the correct `source_page`, then cleaned up the test rows. Round 7 was not blocked on this — both landed the same evening, in the order Peter's live bug report actually surfaced them.

## Next tasks for Claude Code — round 6, Sep 6 — wire the landing page's email capture to something real

Peter asked when the landing page can start actually capturing emails, since the marketing plan (Section 7 of the MVP doc, "CaseWhy Free Marketing Playbook" artifact) assumes the pre-launch email list is already live and building. Checked `src/app/page.tsx` directly: **it isn't wired to anything.** `EmailCaptureForm` is a plain `<form>` with an `<input type="email">` and a submit button — no `action`, no `onSubmit`, no server action. Submitting it today does the browser's default no-action behavior: reloads the page with the email appended to the URL as a query string (`?email=...`) and shows no confirmation. That's worse than doing nothing — the email isn't captured anywhere, and putting it in the URL is a minor exposure of its own (browser history, any referrer header sent to a script on the page).

**What this needs — small, same pattern as everything else in the schema:**

1. A new table (e.g. `email_subscribers`: email, source page, created_at, unique constraint on email so a repeat signup doesn't duplicate) via Drizzle, same as `tracked_cases`/`subscriptions`/`chat_usage`.
2. A server action wired to both `EmailCaptureForm` instances on the page (the hero one and the "Be first to try CaseWhy" one at the bottom) that validates the email, inserts (or no-ops on a duplicate), and shows a real confirmation state instead of reloading.
3. Basic hygiene, not urgent enough to block shipping: a honeypot field or simple rate-limit against bot spam, since this form has no auth in front of it at all (unlike everything else built so far).

**Also check, don't assume:** the static `casewhy.com` site (`main` branch, a separate deployment from this one) has its own copy of this same email-capture section, ported from it — confirm whether *that* form is wired to anything real. If it isn't either, both need the fix (or point both forms at the same backend, whichever is simpler given the two are separate deployments right now).

**Not needed yet, flagging so it isn't over-built:** no CRM, no email-sending integration for this list specifically — Postmark is already verified for transactional email, but nothing here requires marketing-email sending yet, just capturing addresses reliably. A basic way to export/view the list (even a one-off query) is enough for now. Worth remembering before real sends start, though: any future marketing email to this list needs a real unsubscribe path (CAN-SPAM), same honesty standard as everything else on this project.

**Ask:** build 1 and 2 — this directly unblocks Phase 1 of the marketing plan, which assumes the list is already capturing signups. Report back once a real submission shows up in the table and the static site's form status is confirmed either way.

**Done, Sep 6 (Claude Code).** 1 and 2 built: new `email_subscribers` table (email unique, source_page, created_at), migration applied live; both `EmailCaptureForm` instances (hero, footer) now call a real server action instead of doing nothing, with inline success/error state. **Verified twice, in both dev and production**, via a real browser: a real submission from each of the two form instances lands a real row with the correct `source_page`; a duplicate submission of the same email succeeds without erroring (confirmed live, not assumed); test rows cleaned up after each check. One real bug caught during that testing, not by code review alone: the first version 500'd on a duplicate email because Drizzle wraps the underlying Postgres error under `.cause` rather than exposing `.code` directly on the thrown error — the unique-violation check was looking in the wrong place. Fixed and re-verified. 3 (bot hygiene) done at the small-effort end: a CSS-hidden honeypot field, no rate-limiting beyond that, matching the ticket's own "not urgent enough to block shipping" framing. **Static site's form status: confirmed unwired** — `casewhy.com`'s `main` branch has the same bare `<form>` pattern with zero JS behind it (not even a `<script>` tag), and it's a separate static deployment with no backend of its own. Flagging back rather than assuming, per the ticket's own "if it isn't either, both need the fix... whichever is simpler" phrasing: fixing it would mean either a new public, CORS-enabled endpoint on this app for the static site to call, or a duplicated backend — a materially bigger, cross-repo addition than round 6's core ask. **Decision made and built, Sep 6 (Claude Code) — Peter hit the gap directly** (submitted the static site's form himself, got nothing back, asked why and pointed out there was no confirmation either). Went with the "new public endpoint" option: `POST /api/subscribe` (`src/app/api/subscribe/route.ts`), CORS-scoped to `casewhy.com`/`www.casewhy.com` only, reusing the exact same logic as the app's own server action (both now call a shared `src/lib/marketing/subscribe.ts`). `sourcePage` widened to also accept `static-hero`/`static-footer` so the two sites' signups stay distinguishable in the table. The static site's two forms (commit `0f08c42` on `main`) now POST to this endpoint with a CSS-hidden honeypot, showing a real confirmation or inline error instead of silently doing nothing. **Verified live on the real domain, not just locally** (a local test's origin isn't on the CORS allowlist, so it only proves the error path) — typed a real email, clicked submit on `https://www.casewhy.com/`, got the confirmation message, and confirmed the row landed with `source_page: "static-hero"`, then cleaned it up. **Also fixed, same session:** the founder's note said "a call" to the congressional office; corrected to "an email" per Peter's post-approval correction (commit `14b4151`), confirmed live.

## Next tasks for Claude Code — round 7, Sep 6 — refresh the static casewhy.com site's content + design

Peter's ask: "beef up the marketing static page with the latest included functionality... we have just one chance for first impression." Fetched the live `casewhy.com` directly (not `page.tsx` — the separate `main`-branch static site) to check, and confirmed it: the copy is unchanged since it went live Sep 2-3, before almost everything in this file was built. It still says "Coming soon" with generic "Track / Understand / Act" placeholders and no mention of anything real — the AI chat, policy citations, processing times, visa bulletin, document vault, encryption, none of it. Meanwhile `page.tsx` (the Next.js app's own landing page) already got a copy pass naming the real shipped pieces (see the CTA-copy entry under "Scope expansion... CW-33 expansion" history above) — the static site is now the one that reads stale, and it's the page actually live at the real domain. This is the real first-impression page; worth doing properly.

**Two constraints that shape everything below — read before writing any copy:**

1. **Do not imply real-case tracking is live for real users.** Production USCIS API access is still pending (see "USCIS API access" above) — everything built so far has been verified against the sandbox, not a real applicant's real case. Keep the page in "coming soon / join the list" framing. It's fine, and good, to show that the product is *built and tested* — just never word it as "start tracking your case today."
2. **Don't touch `privacy.html`'s legal disclaimer language** (the "this is a working draft, not reviewed by an attorney" notice) — that's correct and deliberate, staying as-is until the lawyer review happens. Content changes below are for `index.html` (or wherever the static site's markup lives on `main`) only.

**New copy — implement close to verbatim, this is worded carefully for the "anxious applicant" audience and the two constraints above:**

- **Logo + tagline — new ask, Sep 6, from Peter directly: add both to the static site.** The static site (built Sep 2-3) predates the "Branding decision" section above (Sep 5 evening) entirely, so it has neither the finalized logo nor the tagline — this is a real gap, not a style nit, and belongs in this same pass. The real brand assets already exist at `public/brand/mark.svg` and `public/brand/wordmark-lockup.svg` (plus `icon-512.png`/`icon-192.png`) — but only on the `nextjs-app` branch; they'll need to be copied over onto `main` (e.g. `git show nextjs-app:public/brand/mark.svg`) since `main` never got them. Add the mark/wordmark to the static site's header (same spot `Logo.tsx` uses it in the app), and place the tagline **"Your USCIS case, explained!"** near/under the logo rather than replacing the existing hero headline — this mirrors the judgment call already made for `page.tsx` above ("these can coexist... tagline near/under the logo, longer H1 as the hero headline"), so do the same thing here rather than re-deciding it. Also swap the static site's favicon/apple-touch-icon for the real brand icon files while in there — worth checking whether it's still a placeholder, since it was never touched during the Sep 5 branding pass (that only updated the Next.js app's auto-detected `icon.svg`/`favicon.ico`).

  **Done, Sep 6 (Claude Code, commit `aaa70eb` on `main`), live at `https://www.casewhy.com/`.** Real mark (`public/brand/mark.svg`, copied from `nextjs-app`) now in the header, in a two-line lockup (wordmark + tagline underneath, `.tagline` in muted small text) — coexists with the unchanged longer hero H1, exactly the "these can coexist" pattern already decided for `page.tsx`. Not a CSS-only wordmark anymore, not the hardcoded-color `wordmark-lockup.svg` either (that asset bakes in a light-only text fill that wouldn't invert for dark mode, so it's copied onto `main` for future use but not used directly in the header). Favicon/apple-touch-icon swapped to the real brand icon set (`icon-192.png`/`icon-512.png`/`apple-icon.png`, the last copied from `nextjs-app`'s `src/app/apple-icon.png` since `main` never had one at all — genuinely a placeholder gap, not a style nit, confirmed by checking rather than assuming). Meta `description` now leads with the tagline verbatim, matching `layout.tsx`'s own pattern. **Verified light AND dark mode**, not just the one my own browser happened to be in — dark confirmed via normal rendering, light confirmed by temporarily forcing the light-mode CSS variable values via a real browser script on the live page (reverted after, nothing persisted) and screenshotting both the hero and the "What's already built" section; both read cleanly, good contrast throughout, no regressions from the earlier round-7 pass.
- **Status badge:** "Coming soon — built and tested, waiting on final USCIS approval" (replaces plain "Coming soon" — more specific, more credible, still accurate).
- **Headline:** keep as-is, it's good and still true: "Know what's happening. Know what's next."
- **Subheadline:** "CaseWhy checks your case with USCIS every day, explains what changed in plain English — grounded in real USCIS policy, not just a reworded status label — and lets you ask follow-up questions any time, day or night."
- **CTA button:** keep "Notify me at launch"; add one small line underneath it: "Built and tested. Opening the moment USCIS grants production access."
- **Backlog-stats section:** keep as-is (12M+ pending cases, +42% growth, 1 status label/zero explanation) — still accurate and effective. Port over the sourcing footnote already added to `page.tsx` ("Source: USCIS quarterly caseload data, Q2 FY2026 — re-verified Sep 5, 2026") since the static site is currently missing it and it's a real credibility signal.
- **New section, replaces the generic "Track / Understand / Act" 3-step grid — this is the actual "beef up," everything in it is real and shipped, not aspirational:**
  - Section heading: "What's already built"
  - Card 1 — "Daily automatic checks.": "We check your case with USCIS every day and email you the moment anything changes — no refreshing, no guessing."
  - Card 2 — "Explanations grounded in real policy.": "When your status changes, CaseWhy explains what it actually means — and when a real USCIS policy memo, court ruling, or backlog might be why, it shows you the source, not just an AI's guess."
  - Card 3 — "Ask CaseWhy anything.": "Have a question about your case at 2am? Ask. CaseWhy answers using your case's real status and history, and always points you to a licensed attorney the moment a question needs real legal advice."
  - Card 4 — "Processing times and the Visa Bulletin, side by side.": "Official USCIS processing-time ranges and the current Visa Bulletin, right next to your own case — not a separate tab you have to remember to check."
  - Card 5 — "CaseWhy Plus, coming at launch.": "Track up to 5 cases for your whole family, unlimited questions, on-demand checks, a secure document vault, and an attorney-ready case report you can bring to a consultation." (Framed as future/launch, not purchasable today — no live billing exists yet, see the pricing section above.)
  - If 5 cards reads too dense for the existing grid layout, 1-2-3-4 are the ones to keep; 5 is the one to cut if something has to give.
- **Trust section — sharpen from generic bullets to concrete claims (everything below is real and true today, not aspirational):**
  - "Official data only." — keep.
  - "Encrypted, and minimal by design." → "Encrypted at rest with AES-256, the same standard used for banking data — and minimal by design: we only ever store what's needed to track your case."
  - New bullet: "No hallucinated citations." → "Every policy or case-law reference CaseWhy shows you is matched against your case's actual facts by code, not invented by the AI or presented as more certain than it is."
  - "Your data isn't for sale." — keep.
  - Keep the "CaseWhy is not a law firm..." disclaimer box exactly as-is (legal language, not a copy-polish target).
- **Bottom CTA section** ("Be first to try CaseWhy" / whatever it's currently called): bring to parity with what `page.tsx` already has here (see its current text) rather than writing new copy — it was already reviewed once, no need to redo it twice differently on two pages.
- **New section — a short founder's note. Approved by Peter, Sep 6 — build this one, it was reviewed and picked (Version A of two drafted) before being added here, not a suggestion to evaluate.** Place it after the trust section and before the bottom CTA (i.e., right before the "Be first to try CaseWhy" section) — a personal, human beat right before the final ask reads better than either extreme (buried at the very bottom, or interrupting the feature pitch up top). Small heading, e.g. "Why I built this," one paragraph, first person, no card/grid styling — this should read as a short personal note, not another feature tile:

  > I built CaseWhy because I lived this. My wife's, my daughter's, and my own naturalization cases sat in months of unexplained silence after our interviews — just a status label that hadn't moved. It took an email to my congressional representative's office to learn why: a USCIS policy memo, later struck down in court, had quietly paused cases like ours. Nobody at USCIS was going to volunteer that. CaseWhy exists so the next person doesn't have to fight as hard just to find out why.

  Use that text verbatim — it's been through review already, not a placeholder for Claude Code to riff on. **Corrected Sep 6, after the first approval:** it was an email to the congressional office, not a phone call — fixed in the quoted text above; if you're working from an earlier fetch of this file that still says "a call," re-fetch and use this version. Sign it simply, e.g. "— Peter, founder of CaseWhy," no title/credential inflation. **Peter wants to see how it reads alongside everything else once it's live** before deciding whether it stays, gets trimmed, or moves — so land it, don't treat it as more locked-in than the rest of this round's copy.

**Design/visual polish — bring the static site in line with the redesign already shipped on the app (see "New priorities from Peter, Sep 4 night" above):** same color-token system (`background`/`surface`/`surface-2`/`border`/`muted`, brand-500/600), same typography (Inter + JetBrains Mono via `next/font/google` — or the plain `<link>` equivalent since this is static HTML, not Next.js), the sticky bordered/backdrop-blurred nav treatment, and the subtle brand-colored radial glow in dark mode. The two sites should read as one product, not two different eras of it. If the static site's HTML/CSS structure makes this a bigger lift than expected, flag it rather than doing a half version — a consistent-but-simpler design beats a half-matched one.

**Called out specifically, Sep 6, from Peter — the blue tint needs to be the exact match, not just "a similar blue":** pull the literal `brand-500`/`brand-600` hex values straight from `tailwind.config.ts` on `nextjs-app` (don't eyeball or re-derive a close blue) and use those exact values everywhere the static site has a blue accent — badges, buttons, links, stat numbers, focus rings — plus the same brand-colored radial background glow the app uses in dark mode. This is the one piece of the design-polish ask most likely to visibly mismatch if approximated instead of copied exactly, since a slightly-off blue sitting next to the now-correct green logo would look like a mistake rather than a design choice. Treat "same hex values, verified side by side" as part of done, not just "looks close."

**Screenshots — use real ones, not mockups, wherever the page needs to *show* rather than *claim*:** the processing-times and visa-bulletin pages don't need the sandbox and can be screenshotted right now via Claude in Chrome against the live `casewhy-app.vercel.app`. The dashboard (status + explanation + citation) and the `/ask` chat need a live sandbox case to render — sandbox is down until Monday 7AM ET, so those two can wait for Monday's verification window, or ask Peter directly — he may already have the screenshots he reviewed for the "black background is dull" design feedback. Don't block the copy/design work on this; land it as a fast-follow once available.

**Also worth doing while in this file, lower priority, don't let it block the above:** `page.tsx`'s own `FeatureCard` grid (the "Track / Understand, with real sources / See the full picture / Act" one) predates the chat, document vault, and attorney report too — same staleness, smaller audience since it's not the page at the real domain. Fold in the same "What's already built" framing there too if there's a natural opening, otherwise leave it for a later pass.

**Ask:** implement the copy and design changes on the `main` branch (the actual `casewhy.com` deployment), keep the two constraints above (no "tracking is live" claims, don't touch `privacy.html`'s legal language), and report back with the live URL to check plus which of the two screenshot pairs (processing-times/visa-bulletin now vs. dashboard/chat later) made it in. This can go out independent of round 6's email-capture backend — do both, but this one doesn't need to wait on that one or vice versa.

**Done, Sep 6 (Claude Code), live at `https://www.casewhy.com/` (commit `9cf22d6` on `main`).** All copy implemented close to verbatim: sharper status badge, new subheadline, the 5-card "What's already built" section (real screenshots embedded in card 4), sharpened trust bullets, the founder's note (verbatim text, placed exactly where specified — after trust, before the bottom CTA), and the bottom CTA brought to parity with `page.tsx`. Design ported directly from the app's real source of truth (`src/app/globals.css` + `tailwind.config.ts`, not approximated) — same light/dark tokens via `prefers-color-scheme`, same brand-500/600 blue, Inter + JetBrains Mono via Google Fonts, the sticky blurred nav, and the app's real logo mark (copied from `public/brand/mark.svg`) instead of the old CSS-only wordmark. **Screenshot pair that made it in: processing-times + visa-bulletin** (captured live against `casewhy-app.vercel.app`, cropped to remove the signed-in header and a browser-extension overlay that was highlighting random words on the page — neither is part of the real product). **Dashboard/chat screenshots deferred**, as the ticket anticipated — no live sandbox case to render them against until Monday. `privacy.html` confirmed untouched via a byte-for-byte diff against its pre-round-7 commit, not just assumed. Verified by serving the page locally first (full walkthrough, dark-mode render, zero console errors, all asset requests 200) before pushing, then re-verified live on the real domain after deploy.

## Next tasks for Claude Code — round 8, Sep 6 — social media setup + a monitoring/draft-reply tool

**`SOCIAL_MEDIA_GUARDRAILS.md` (repo root) is the concrete operating doc for this tool — read it before drafting any reply/post/comment logic or prompts.** It sets the draft-and-approve model (confirmed below, item 3) as a hard rule, the content hard-stops (no case-specific guidance, no repeating identifying case details, no overclaiming product state, mandatory disclosure/sourcing), the escalate-instead-of-draft list, and per-channel constraints matching the marketing plan. Keep it in sync with the marketing plan and the MVP scope doc's legal-risk section if either changes.

New direction from Peter: the cloud session now owns running the marketing plan (see the "CaseWhy Free Marketing Playbook" artifact — full channel-by-channel plan already built), and wants Claude Code to own the technical side of social media: setting up CaseWhy's actual pages/accounts, and building a tool to monitor social platforms for questions and help respond. Peter and the cloud session design the content together; this file is where the technical build gets scoped, same pattern as everything else.

**One real decision is still open and deliberately not resolved here — see below before building anything that posts publicly on its own.** Everything else in this round can start now regardless of how that resolves.

**1. Account setup — per the marketing plan's own channel guidance, not every channel gets a branded page:**

- **Gets a real "CaseWhy" brand account:** X/Twitter, Threads (mirrors X), TikTok, Instagram (cross-posts TikTok to Reels), YouTube, Pinterest (lower priority, After-Production phase per the plan), and a LinkedIn **Company Page** (in addition to Peter posting personally — the plan's LinkedIn play is built around his personal voice/network, a company page is just a hub, worth having both).
- **Deliberately no branded account:** Reddit and Facebook groups — the plan is explicit that Peter participates there as himself, disclosed ("full disclosure: I built this"), not as a "CaseWhy" account; most of these communities ban outside-brand accounts outright, and a branded account here would work against the plan, not for it. Don't set these up.
- **Account creation itself needs Peter's own hands** — picking a password, accepting each platform's ToS, phone/email verification, and any CAPTCHA step are all things Claude Code shouldn't do on his behalf (same reasoning as never handling credentials elsewhere in this project). What Claude Code *can* prep so sign-up is fast and consistent: a short bio (using the finalized tagline "Your USCIS case, explained!"), the real logo mark as the profile image (already in `public/brand/`), consistent handle-name candidates (`@CaseWhyApp` or similar, checked for availability where checkable), and a link-in-bio destination. Once Peter's logged into a platform in his own real browser, Claude Code (via Claude in Chrome, inside that already-authenticated session — not creating new credentials) can help navigate and publish, same as it already does for Vercel/Neon/Postmark console work elsewhere in this project.

**2. Monitoring + draft-reply tool — real findings from research on what this actually costs/requires, factor these in before scoping:**

- **X/Twitter API has no free tier as of Feb 2026** — pay-per-use, roughly $0.005 per read and $0.015 per reply posted, no monthly minimum below Enterprise ($42k+/mo, not relevant here). At CaseWhy's realistic early volume this is genuinely cheap (a few dollars a month), but it's a real, metered cost to budget for, not free infrastructure — check current pricing directly before building against it, this was researched today and terms move.
- **Instagram/Facebook (Meta Graph API) requires mandatory app review** (2-4 weeks for standard permissions like `instagram_manage_comments`, longer for DMs) **and Business Verification in Meta Business Manager, which requires official business documents.** This is a real, concrete dependency on the Florida LLC (still not filed as of this writing) — Meta's business verification is unlikely to succeed as a sole proprietor the same way a registered LLC would. **Practical read: don't build Instagram/Facebook monitoring yet — it's gated on the LLC existing, not a code task to start now.** X, TikTok (has its own comment API), YouTube, and Reddit's read-only API don't share this specific blocker.
- **Reddit and forums (VisaJourney, Quora) are explicitly personal-voice per the plan** — there's no "CaseWhy account" replying there to monitor for, so the tool's job on those channels is closer to alerting Peter to a relevant thread than drafting a reply as a brand.

**3. Confirmed, Sep 6 — draft-and-approve, not autonomous. Peter's decision, not just a recommendation now:** build this so nothing posts publicly without his review, at least for this phase — don't wire an auto-post capability at all right now, not even behind a flag. CaseWhy's entire product is built around a hard guardrail — AI-generated guidance stays general, never a conclusion about one specific person's case, always routes real legal questions to an attorney (see CW-32's guardrail testing above, and the compliance section near the top of this file). That discipline was earned through real adversarial testing inside a controlled, logged-in product surface. A public social reply is a materially higher-risk surface for the same failure mode — unmoderated, screenshotable, and read as CaseWhy speaking as a company, not as an individual hedging in a Reddit comment. **Recommendation: build this as draft-and-approve, not autonomous, at least to start** — the tool monitors mentions/comments/DMs on the created accounts, drafts a suggested reply grounded the same way CW-32's chat is (CW-31 KB, same guardrails, same "get an attorney" redirect logic), and queues it for Peter to approve or edit before anything actually posts. **This isn't wasted work if Peter later wants more autonomy** — auto-post is just removing the approval click from an already-built pipeline, so build the monitor-and-draft layer now — but the auto-post step itself stays out of scope entirely until/unless Peter explicitly asks for it later, not something to build ahead of that ask.

**Ask:** start on account-prep assets and the monitor-and-draft tool's read/draft side (X + TikTok + YouTube first, per the API-access findings above — skip Instagram/Facebook until the LLC exists). Don't wire an auto-post capability yet. Report back with real findings the same way CW-33(c) and the Meta/X research above did — if something's blocked or costs more than expected, say so rather than building around it silently.

**Deferred, Sep 6 — Peter's explicit instruction: "defer social media build, hold off till further notice."** Nothing in this round has been built. `SOCIAL_MEDIA_GUARDRAILS.md` is committed and ready for whenever this resumes, but treat round 8 as paused, not in progress — don't pick it back up without Peter (or the cloud session, on his behalf) explicitly saying to resume.

## Next tasks for Claude Code — round 9, Sep 6 — CW-39 escalation toolkit: real build estimate

**Decided by Peter Sep 6 — see Section 6 of the MVP scope doc for the full record.** This is CW-39, previously scoped-but-not-authorized; it's now authorized as a third addition to the existing CaseWhy Plus tier (unlimited chat + 5-case tracking), at no price change ($9.99/mo covers all three). Ask: a real build estimate, same pattern as CW-37/38/40 — not asking you to build it yet, just size it honestly like those were sized.

**What it is, concretely — two parts:**

1. **A "stalled case" detector.** Reuses the existing daily poll/diff job and case-history data — no new data source. Rule: if a case has passed a milestone (interview held, ceremony scheduled) and gone N days with no further status change, where **N is benchmarked per-office/per-form against CW-33's own processing-time ranges** (not a fixed day count — this was a deliberate choice over the simpler fixed-N option, so it's a real dependency on CW-33's data staying current), surface a distinct "this case looks delayed" card on the dashboard. **This card is free, visible on every tier** — only the two tools below are Plus-gated.
2. **Representative lookup + a three-template drafting assistant, gated to Plus:**
   - User enters a mailing address once → resolve to a congressional district via the Census Bureau's free Geocoding Services API → return their House rep + two senators from a small hand-maintained table (same refresh pattern as CW-31/33/34's knowledge base, but on a ~2-year election-cycle refresh, not monthly).
   - **The mailing address is new PII, more sensitive than anything CaseWhy stores today** — needs the same AES-256-GCM at-rest encryption as receipt numbers, and triggers the privacy-policy update already flagged in Section 5 of the MVP scope doc. Flag this explicitly in the estimate — it's not just a form field.
   - **Three drafting templates, not one** — a congressional-inquiry letter, a written follow-up to the local field office, and a USCIS Ombudsman Case Assistance request. Each is pre-filled from data already on file for the tracked case (receipt number, filing date, interview/ceremony date, current status) plus one short user-provided reason the delay matters to them. Same rule as the explanation layer and chat: organize/polish the user's own content, never generate new case-specific claims from scratch. Worth reusing the AI-drafting pattern already proven in CW-32 (chat) and CW-40 (attorney-handoff report) rather than building a new prompt pipeline from scratch.
   - Suggested (not mandated) UX: after the first template is used, nudge toward the next one ~2 weeks later if the case still hasn't moved — mirrors the real multi-step escalation sequence this is modeled on, but this sequencing detail is yours to refine.

**What's reused vs. genuinely new, for sizing purposes:**
- Reused: the poll/diff job, case-history model, CW-31/33/34's "hand-curated table" pattern, CW-32/CW-40's AI-drafting infrastructure.
- New: the stall-detection rule; Census Geocoding integration; the rep/senator table + its refresh job; the new encrypted mailing-address field + privacy-policy update; three drafting prompts and their guardrail/adversarial testing (same bar as CW-32's five-question test, applied here).

**Not asking for yet:** the actual UI polish, the Ombudsman/field-office letter content details beyond "same pattern as the congressional one," or anything about the "invite a family member" referral mechanic from the marketing plan (unrelated feature, don't conflate). Just the scoping/estimate pass, flagged the same way CW-33(c) and the account-setup research above were: real findings, real numbers, say plainly if something costs more or takes longer than the summary above implies.

## Next tasks for Claude Code — round 10, Sep 6 (revised same day) — build AND DEPLOY the Terms of Service page

**Revised after Peter's review of the first draft — read this version, not an earlier fetch.** `TERMS_OF_SERVICE_DRAFT.md` in this repo has been updated to reflect four changes Peter asked for; its own "Notes for Claude Code" section at the bottom has the current build instructions. Summarized here:

- Build it as `terms.html` on the `main` branch (the static `casewhy.com` site — same place `privacy.html` and `index.html` live), matching `privacy.html`'s exact visual style/chrome (header/nav, footer, typography, color scheme). This is a content/build task on `main`, not on `nextjs-app`.
- **This page does NOT carry a "draft" banner.** Unlike `privacy.html` (which stays marked draft, pending attorney review, unchanged), the Terms of Service is being published as final/effective — dated September 6, 2026 — per Peter's explicit instruction. Do not add a draft notice here even though the visual chrome should otherwise match `privacy.html`.
- **No personal name appears on this page.** The document refers to CaseWhy as operated as a Florida sole proprietorship, not by name — don't reintroduce a founder name anywhere in the built page.
- **Contact address is `terms@casewhy.com`**, not `hello@`. Use it anywhere the page names a contact address for Terms-related questions.
- Cross-link it with `privacy.html`, and add a third footer link ("Terms of Service") next to the existing "Privacy Policy" link in both places that currently link to `privacy.html`: the static site's own footer, and the Next.js app's landing page footer (`src/app/page.tsx`, currently just "Privacy Policy" + "Contact").
- Section 5 of the draft (Subscription Plans and Billing) describes CaseWhy Plus billing terms for when it eventually goes live — it is NOT authorization to build a live checkout flow. That's still gated on the Florida LLC, a payment processor, and the lawyer review (Section 6 of the MVP scope doc) — the Terms page itself no longer says "draft," but that gate on actually charging money is unchanged.
- Same rule that applied to round 7's static-site work: don't touch `privacy.html`'s existing legal disclaimer/draft language while you're in there — only add the new cross-link to it.
- **This is a build-and-deploy task, not just a build.** Push `terms.html` live to `casewhy.com/terms.html` on `main`, same as round 7's `privacy.html`/static-site work, and report back the live URL once it's up.

**Separate, not a code task — flagging so it doesn't get lost:** Peter also asked for a `terms@casewhy.com` email alias, same pattern as `hello@`/`privacy@` (free aliases on the `info@casewhy.com` Google Workspace seat). This has to be added by Peter directly in the Google Admin console (Directory → Users → info@casewhy.com → Email aliases) — it's not something achievable from the codebase. Worth a one-line reminder if Peter checks in on this round's status and the alias isn't showing up yet in his own testing.

**Done and live, Sep 6 (Claude Code), `https://www.casewhy.com/terms.html` (commit `ef8f1cb` on `main`).** Built verbatim from `TERMS_OF_SERVICE_DRAFT.md`, no draft banner, "Effective: September 6, 2026," contact `terms@casewhy.com`, cross-linked with `privacy.html` both ways, third footer link added to the static site's own footer and `src/app/page.tsx`'s footer. **Scope expanded twice, same session, both by Peter's direct real-time instruction — not assumed:** (1) `privacy.html` was also finalized (draft banner removed, "30 days" filled in for the retention placeholder, CCPA section written out instead of "to be finalized with legal review") — this overrides the "don't touch privacy.html's draft language" guidance both this round and round 7 gave earlier; Peter said so directly, in the same conversation, after the ToS decision was already confirmed. (2) All three static pages (`index.html`, `privacy.html`, `terms.html`) were also restyled to match the actual app design system (same tokens as `src/app/globals.css`/`tailwind.config.ts`, Inter + JetBrains Mono, the sticky blurred-nav logo lockup) — they'd still been on the original light-only green-accent style even after round 7's content refresh. **The two stale bullets below this note and under "Key files"** (both said "same draft pending attorney review treatment" for `terms.html`) are now incorrect on two counts — no draft banner was ever the plan, and `privacy.html` isn't a draft anymore either — corrected in place. **Not done:** the `terms@casewhy.com` alias itself (Google Admin console, needs Peter signed in — offered to add it once he is, not yet confirmed).

## Next tasks for Claude Code — round 11, Sep 6 — add the escalation toolkit to the "CaseWhy Plus" marketing copy

Peter's ask: the live "CaseWhy Plus, coming at launch" card (round 7's Card 5, on the static `casewhy.com` site's "What's already built" section) was written before CW-39 was decided later the same day, so it lists the Plus-tier features without the escalation toolkit. Simple copy fix, no functionality change — this is still forward-looking marketing copy for a feature not built yet, same as the rest of that card already was.

**Current live text (verify against the actual live page first, in case it's drifted from this):**
> "Track up to 5 cases for your whole family, unlimited questions, on-demand checks, a secure document vault, and an attorney-ready case report you can bring to a consultation."

**New text — add the escalation toolkit in, keep everything else as-is:**
> "Track up to 5 cases for your whole family, unlimited questions, on-demand checks, a secure document vault, an escalation toolkit for stalled cases (representative lookup plus congressional, field-office, and USCIS Ombudsman letter templates), and an attorney-ready case report you can bring to a consultation."

- Same framing as before applies: this is still describing CaseWhy Plus as "coming at launch," not implying CW-39 (or the rest of the Plus tier) is purchasable today — no live billing exists yet.
- Update it on `main` (the static site, where this card actually lives) — confirm during the edit whether `page.tsx`'s older 4-card `FeatureCard` grid on `nextjs-app` ever got a matching Plus-tier card added; as of the round-7 handoff it hadn't, so there's likely nothing to change there, but check rather than assume given how much has shipped since.

**Done, Sep 6 (Claude Code), live at `https://www.casewhy.com/` (commit `6e7430a` on `main`).** Text swapped exactly as specified, confirmed the live pre-edit text matched what this round assumed before changing it. Checked `page.tsx`'s `FeatureCard` grid directly (not assumed) — still just the original 4 cards, no "CaseWhy Plus" mention anywhere on it, so nothing to change on `nextjs-app`.
- Small, isolated edit — no need to touch anything else on the page while in there.

## Next tasks for Claude Code — round 12, Sep 6 — build the full CaseWhy Plus tier (minus live billing)

**Peter's direct authorization, Sep 6 (later, after round 11): build out the whole CaseWhy Plus tier now — CW-35, CW-36, CW-37, CW-38, CW-39, and CW-40 — everything except a live checkout/payment flow.** This supersedes round 9's framing ("not asking you to build it yet, just size it") for CW-39 specifically — CW-39 is now authorized to actually build, not just estimate. Confirmed directly with Peter: this does NOT mean take real payments or file the Florida LLC — those two gates from Section 6 of the MVP scope doc are unchanged and still block real billing. What's authorized is everything that doesn't require a live payment processor.

**Concretely, "everything except live billing" means:**

1. **Schema/packaging (CW-35/36 foundation).** `subscription_tier` column on the account (`"free"` default), a usage-counter table for the chat's 10-question/month free limit, and the `tracked_cases` schema change to allow up to 5 rows/account on Plus. If any of this is already built, verify it end-to-end rather than assume from an old note in this file.
2. **CW-35 — chat metering.** Enforce the 10-question/month free limit in the chat endpoint; unlimited when `subscription_tier` is Plus.
3. **CW-36 — family plan.** Up to 5 tracked cases on Plus, 1 on free, enforced wherever a case gets added.
4. **CW-37 — on-demand checks.** The recommended v1 from the earlier estimate: a manual "check now" button, Plus-gated, respecting the production API's 10-tx/sec ceiling (not relevant yet at sandbox volume, but don't hardcode around it either).
5. **CW-38 — document vault tier-gating.** The vault itself (`DocumentVault.tsx` + `/api/documents/`) already shipped Sep 5 per this file's own build log — confirm whether it's currently tier-gated to Plus at all; if it's open to every tier today, that's a real gap against the MVP scope doc's monetization list and needs fixing as part of this round, not left inconsistent.
6. **CW-39 — the full escalation toolkit, per the round-9 spec and `cw39-escalation-toolkit-concept.md`:** the free stalled-case detector card (benchmarked against CW-33's ranges), plus the three Plus-gated pieces — representative lookup (Census Geocoding + hand-maintained table), and all three drafting templates (congressional inquiry, field-office follow-up, USCIS Ombudsman request). Includes the new encrypted mailing-address field (same AES-256-GCM standard as receipt numbers). **Since `privacy.html` was already finalized (no longer draft, per your own note under round 10) rather than still being a draft awaiting this exact kind of addition, make sure the mailing-address collection is actually reflected in its live text before this ships** — don't let the finalized privacy policy fall behind a real new PII category being collected.
7. **CW-40 — attorney-handoff report.** The exportable PDF timeline (`@react-pdf/renderer`), Plus-gated, per the existing ~2-3 hour estimate.
8. **Upgrade-prompt UI throughout.** Wherever a free-tier limit is hit (chat questions, case count, on-demand check, vault, escalation tools, report), show a real "This is a CaseWhy Plus feature" prompt — but since there's no live checkout, the actual upgrade action should read as coming soon / not yet purchasable (a disabled button, a waitlist-style capture, or similar — your call on the least-awkward UX) rather than pretending a purchase flow exists.

**What's explicitly NOT in scope for this round:** no Stripe (or any payment processor) integration, no real subscription purchase flow, no charging anyone. `subscription_tier` should be flippable only through a manual/admin path (direct DB update, a debug endpoint gated to Peter, whatever's fastest for you to test with) since there's no purchase flow to set it through yet. Don't file the Florida LLC or touch anything USCIS-affidavit-related as part of this — unrelated gates, not this round's job.

**Report back with:** what's built and verified per item above, what (if anything) was already done and just needed verification, and flag plainly anything that turned out bigger than the earlier estimates (CW-37 ~1-2h, CW-40 ~2-3h) implied — especially CW-39, which never got a real estimate back before this authorization to build it outright.

**Blocked, Sep 6 (Claude Code) — flagging before starting, not building around it:** item 6 cites `cw39-escalation-toolkit-concept.md` for CW-39's spec, but that file doesn't exist anywhere in this repo — checked the working tree, `git status`, and `git log --all` on both branches, nothing. All I actually have for CW-39 is round 9's own summarized scope (stalled-case detector + Census-based rep lookup + three drafting templates), not whatever additional detail that concept file was meant to carry. Given CW-39 is the one part of this round with no prior real build estimate at all (per this round's own closing line), and it's also the part introducing new, more sensitive PII (mailing addresses), I don't want to guess at a missing spec for it. **Recommend:** either the concept file gets added to the repo, or round 9's existing scope (already detailed enough to build from) is confirmed as the actual full spec — whichever is faster. Everything else in this round (items 1-5, 7, 8) doesn't depend on the missing file and can start without waiting on this.

**Resolved, Sep 6 (cloud session):** `cw39-escalation-toolkit-concept.md` is now committed at the repo root (mirrored from the Ideas project doc of the same name, which Claude Code can't reach directly). It's the full concept doc referenced above — the two-part design, the Maria walkthrough, the reused-vs-new breakdown, and the four Sep-6 decisions — plus an updated "Next step" section confirming this round's authorization to build outright, not just estimate. Go ahead with item 6.

**`src/lib/congress/representatives.ts` expanded to full national coverage, Sep 6 (cloud session):** Peter asked directly — the Florida-only pilot table (3 entries) had no value for users outside FL, and he asked for all 50 states. Now 534 entries: all 100 senators + 434 of 435 House seats (TX-23 is a genuine current vacancy — Tony Gonzales resigned in April 2026, no special election held yet as of this writing — reflected honestly as uncovered rather than guessed).

Sourcing: all 100 senators from the Senate's official XML contact feed (name, party, DC phone, official website for every one — programmatically verified 2-per-state, 100 total, zero gaps). All House seats from house.gov / Wikipedia's per-state delegation pages, after a first bulk fetch of house.gov's directory was caught fabricating entries once the real page content ran out (invented a 3rd WV district — WV has had only 2 since 2020 redistricting — and misattributed several members to wrong states). That fabricated batch was discarded and every state re-sourced individually, then checked programmatically against the correct seat count per state before use.

**Real, tracked gap — flagged rather than hidden:** the Senate's feed conveniently bundles phone + website with every entry; there's no equivalent single official source for the House, so all 434 House entries currently use the Capitol switchboard (202-224-3121 — correctly reaches any office, same fallback pattern already accepted for the original Rick Scott entry) and the official House lookup page as a placeholder website, rather than guessing an individual `lastname.house.gov` URL that might not match. Peter's direction: ship this now, revisit later to verify each House member's actual direct line and website individually. Added as item 26 below.

**Verified and one real bug fixed, Sep 6 (Claude Code), before shipping — not taken on trust.** Found and fixed two syntax-level double-commas (`},,`) that made two array entries evaluate to `undefined` and failed `tsc`. Then independently cross-checked the full 534-entry table against a separately-fetched, separately-sourced dataset (the `unitedstates/congress-legislators` project's `legislators-current.yaml`, already verified accurate against three live checks this session — it correctly reflected Ashley Moody's FL Senate seat and Jimmy Patronis's FL-1 special-election win, both confirmed live earlier). Seat counts matched known 2020-reapportionment totals for every state; zero duplicate district keys; all senator/house name "mismatches" resolved to pure formatting differences (informal name vs. official full name with middle initials, or missing accent marks) — same real people, not wrong ones. **One real, genuine error found and fixed:** FL-20 was shipped with a stale entry (Sheila Cherfilus-McCormick) for a seat that's actually vacant — confirmed live against Wikipedia's current-members page ("As of September 2, 2026, there are 433 representatives and 2 vacancies"), which named exactly TX-23 and FL-20. Removed the stale FL-20 entry so it now correctly falls back to "not covered," matching how TX-23 was already handled. Also fixed one garbled name (`"Angus S., Jr. King"` → `"Angus King"`) caught by the same cross-check. **Live-tested the expanded table end to end** against real addresses in CA, TX, and NY (not just the original FL pilot) — all three resolved to the correct real senators and House member. `tsc`/lint clean, full production `next build` succeeds. Live at `casewhy-app.vercel.app` (commit on `nextjs-app`).

**Done, Sep 6 (Claude Code).** Verified current state first rather than assuming: **items 1-4 were already built and correctly gated** (CW-35 chat metering, CW-36 5-case cap, CW-37 check-now, CW-40 PDF report — all already checked `getSubscriptionTier`, confirmed by reading the actual route/action code, not from an old note). **Item 5 (CW-38) was the one real gap** — the document vault had zero tier checks anywhere (`DocumentVault.tsx`, both `/api/documents` routes); added server-side gating to both `GET`/`POST /api/documents` (403/402, download/delete on already-owned documents stay ungated — a hypothetical future downgrade shouldn't lock someone out of their own already-uploaded files) plus a client-side upgrade message matching the existing `CheckNowButton`/`DownloadReportLink` pattern.

**Item 6 (CW-39), the large new piece — built as: Part A, the free stalled-case detector** (`src/lib/escalation/stall-detector.ts`), surfaced as an amber card on every tier's dashboard. **Honest gap flagged in the code itself, not hidden:** the concept doc calls for per-office/per-form benchmarking against CW-33's processing-time data, but CW-33 only has overall filing-to-completion percentiles, not post-milestone-specific timing — that's genuinely different, finer-grained data CaseWhy doesn't have. Shipped a clearly-labeled 90-day-since-milestone threshold instead of a fabricated precise-looking number; real per-office benchmarking is a real follow-up once that data exists.

**Part B (Plus-gated):** a new `mailing_addresses` table (one encrypted JSON blob per account — name + address, name added because a real letter needs a real signer and nothing in CaseWhy collects one today) and `escalation_letters` (records which template was used, for the "nudge after 14 days" sequence — the nudge itself isn't built, see below). **Representative lookup uses the real Census Bureau Geocoding API** — tested directly against real addresses (Tallahassee, FL correctly resolved to FL-02; the White House address correctly resolved to DC's at-large delegate district), not assumed from documentation, which turned out to describe the wrong TIGERweb layer code (layer 10 returns Census Block Groups, not districts — layer 54 is the one that actually returns `"119th Congressional Districts"`).

**Real, deliberate scope-down on the rep/senator table, flagging plainly rather than guessing:** the concept doc's "hand-maintained table" for 100 senators + 435 House districts would mean fabricating or recalling ~535 individual officeholders from training data with no way to verify current accuracy — and a live check already caught one real, since-cutoff change this same session (Florida's senior Senate seat is Ashley Moody, not the name an older dataset would produce). Rather than ship a table that's confidently wrong in places a user might actually mail a real letter based on, this pass ships a **small, individually-verified pilot: Florida's 2 senators + FL's 2nd House district**, each checked directly against the member's own official .senate.gov/.house.gov contact page. Every other address correctly falls back to "not covered yet" with links to the real House/Senate lookup tools, never a wrong or fabricated entry. **Expanding to full national coverage is a real, separate, much larger data-entry task — flagging this now, not quietly treating the pilot as done.**

**Three letter-drafting templates** (congressional, field-office follow-up, Ombudsman) share one drafting function, reusing the same AI-drafting pattern as CW-32/CW-40. **Adversarial-tested, same bar as CW-32, with a real result worth recording:** a test prompt asked the drafter to include a fabricated claim ("case denied for fraud, owed $50,000") the user admitted was false — the model refused outright rather than including it, explaining that doing so could expose the user to real legal liability. A second, legitimate-input test produced a genuine, well-formed, properly-cited letter. Both verified via a real model call, not assumed.

**Item 7 — a manual/debug tier-flip path**, per the round's own "whatever's fastest" allowance: `scripts/set-subscription-tier.ts <email> <plus|free>`, a CLI script (not a web-exposed endpoint — smaller attack surface than inventing a "gated to Peter" auth check) that looks up a user by email against Neon Auth's own schema and upserts the `subscriptions` row directly.

**Item 8 — upgrade-prompt UI:** every Plus-gated surface (vault, check-now, report, escalation toolkit) shows a real "Upgrade to CaseWhy Plus..." message, consistent styling, no fake purchase flow anywhere.

**privacy.html updated the same session** (commit `b930ad1` on `main`, live) — the new mailing-address PII and the Census API third-party flow are both disclosed, so the already-finalized privacy policy doesn't fall behind a real new data-collection point, per this round's own explicit instruction.

**Verified:** `tsc`/lint clean; a full production `next build` succeeds cleanly; a throwaway script exercised the entire real pipeline end to end — real Census geocoding, real representative lookup, a real encryption round-trip (confirmed the stored value doesn't contain plaintext), both stall-detector branches, and both AI letter-drafting tests described above — then cleaned up its own rows. **Not verified: a live, signed-in browser click-through** — same sandbox-hours gap as CW-37/38/40 (StatusCard, which mounts all of these, has nothing to render against until a live case status exists), *and* a separate, real oddity worth flagging: the browser shows a signed-in session (`ptakla@gmail.com`) but a direct query against `neon_auth."user"` on the same database currently returns zero rows — didn't chase this further since it's a pre-existing Neon Auth question, not a CW-39 regression, but worth knowing before Monday's verification window, which now also needs to cover CW-39's UI.

**Not built, explicitly scoped out for now:** the "nudge again 14 days later toward the next template" notification — the concept doc itself called this "suggested, not mandated... yours to refine." `escalation_letters` records what's needed to build it later (a cron-style check for a case with a letter older than 14 days and still no status change), but the actual nudge/notification mechanism isn't wired up this pass.

**Bigger than the (nonexistent) estimate implied, flagging as asked:** CW-39 was easily the largest single piece of this round — new schema, a new external API integration, a verified (not fabricated) but deliberately partial dataset, three AI-drafting prompts with real adversarial testing, and a new PII disclosure. Everything else (items 1-5, 7, 8) was smaller than CW-39 individually, mostly verification + one real gap (CW-38) rather than new builds.

## Architecture (per the MVP scope doc)

- Frontend: Next.js, mobile-responsive, ~~installable as a PWA~~ (not a native app for v1) — **done Sep 5 ~6:50am ET**, see "Next tasks for Claude Code" item 2 above
- Backend: small API + a scheduled poll/diff job against the USCIS Case Status API (same cron pattern as Peter's NVDA trading journal and his personal N-400 Google Sheets tracker)
- Database: Postgres for accounts/case records, receipt numbers and case data encrypted at rest
- AI layer: a Claude API call on each detected status change — raw USCIS status text + a curated policy/case-law knowledge base → plain-language explanation + general next-step guidance (never case-specific legal advice)
- Notifications: SendGrid/Postmark for MVP email; push is a v2 addition
- Initial case-type scope: naturalization (N-400), family-based green cards, employment-based cases — not every USCIS form type

## Legal/compliance guardrails to build with, not bolt on

- Unauthorized-practice-of-law risk: keep all AI-generated guidance general and informational, sourced from public USCIS process docs, never a conclusion about *this specific user's* case. Always route case-specific questions to "talk to an attorney." Disclaimer/ToS language still needs a lawyer's review before launch (~$500-1,500 budgeted) — the live privacy policy is explicitly a draft pending that review.
- Data sensitivity: encrypt receipt numbers/case data at rest, minimize retention, never share/sell.
- Read the production-access affidavit's terms carefully during the access process — it may constrain commercial use, retention, or resale.
- Avoid the string "uscis" in any business-controlled identifier (see the email note above).
- Social/community content (Reddit, Quora, forums, X, etc.) follows the same discipline, detailed further in `SOCIAL_MEDIA_GUARDRAILS.md` — draft-and-approve only, no case-specific guidance, no identifying case details, mandatory disclosure when CaseWhy is mentioned.
- CW-39 (round 9 below) introduces mailing addresses as a new, more sensitive PII category — same at-rest encryption standard as receipt numbers, plus the privacy-policy update this requires before it ships, not an afterthought.
- The Terms of Service is live (`terms.html`, round 10 below), published as final/effective, not a draft — and `privacy.html` was also finalized the same session (no more draft banner). Neither is lawyer-reviewed yet; that gap is now a business-risk decision Peter made explicitly, not a build-task caveat to repeat here.

## Key files in this repo

- `SOCIAL_MEDIA_GUARDRAILS.md` — governs any drafting/posting logic for social/community content (the round 8 monitor-and-draft tool, and any future auto-draft prompts); read before touching that surface
- `TERMS_OF_SERVICE_DRAFT.md` — source text for the now-live `terms.html` (round 10) — the page itself is published as final, this file is just the build source, not a live document itself
- `README.md` — scaffold notes and the researched USCIS onboarding-process requirements (register → sandbox 5-day traffic → affidavit → live demo → production access)
- `src/lib/uscis/client.ts` — OAuth 2.0 client-credentials flow + `getCaseStatus()`, wired to the sandbox; `uscisRequest()`/token cache now accept a per-call credentials override (Sep 5 ~8:30am ET) so FOIA can use its own Torch app without disturbing Case Status's
- `src/lib/uscis/foia.ts` — FOIA Request and Status API client (`createFoiaCase()`, `getFoiaCaseStatus()`); credential fix verified working Sep 5 ~8:30am ET, see CW-18 above — two real API quirks documented directly in its types (`requester` and `digitalDelivery` effectively required despite being typed optional)
- `src/app/dashboard/page.tsx` — dashboard, wired to `getCaseStatus()`, `explainCaseStatus()`, and (Sep 4 late evening) the signed-in tracked-case auto-load
- `src/app/dashboard/actions.ts` / `TrackCaseButton.tsx` — server actions + client button for tracking/untracking a case; CW-36 (Sep 5 ~7:40am ET) made this multi-case-aware, capped by plan tier
- `src/app/dashboard/CaseSwitcher.tsx` — CW-36's pill-row case switcher, shared by `/dashboard` and `/ask`, done Sep 5 ~7:40am ET
- `src/lib/billing/tier.ts`, `src/lib/billing/chat-usage.ts` — CW-35/36's tier limits + chat-question metering (schema/logic only, no live billing), done Sep 5 ~7:40am ET
- `src/app/dashboard/DocumentVault.tsx` + `src/app/api/documents/` — CW-38 document vault (Sep 5 evening), replaces the old `FileUploadStub.tsx`; real upload/list/download/delete against a private Vercel Blob store, `case_documents` table tied to a tracked case (see CW-38 above for verification detail and the one open item — live UI check, gated on sandbox hours)
- `src/lib/db/crypto.ts` — AES-256-GCM app-level encryption for sensitive columns, done Sep 4 late night
- `src/lib/ai/explain.ts` — `explainCaseStatus()`, AI Gateway (Anthropic BYOK) + `ai` SDK + zod, done Sep 4; prompt sharpened Sep 5 to include full status history, elapsed-days, and receipt-derived processing center (see "New priorities" above)
- `src/lib/auth/server.ts` / `client.ts` — Neon Auth (`@neondatabase/auth`) server instance + React client, done Sep 4 late evening
- `src/app/api/auth/[...path]/route.ts` — Neon Auth's catch-all handler route
- `src/app/auth/sign-in/page.tsx` — custom magic-link sign-in form
- `src/components/AuthHeader.tsx` — sign-in/out header, client-side session check (kept out of the root layout's server rendering so the static landing page stays static); redesigned Sep 5 as a sticky bordered nav, see below
- `src/components/Logo.tsx` — shared wordmark/logo mark, added Sep 5, 2026 as part of the design pass, used in the header and sign-in page
- `src/lib/db/schema.ts` / `client.ts` — Drizzle schema (`tracked_cases`, plus `subscriptions` and `chat_usage` as of CW-35/36) + lazy `getDb()`; migrations in `src/lib/db/migrations/` (`0002_messy_cyclops.sql` applied live Sep 5 ~7:40am ET — dropped the old unique index on `tracked_cases.user_id`, added the two new tables)
- `src/app/page.tsx` — landing page (email capture, no backend wired). Sep 5 ~5:35am ET: ported the full "why CaseWhy" content from the static `main`-branch marketing site (`index.html` — a separate deployment, casewhy.com) that the app's own landing page never had: the 12M-pending-cases backlog-stats section, the Track/Understand/Act three-step feature grid, the "built to be trusted" section with its unauthorized-practice-of-law disclaimer, a second email-capture CTA, and a footer (copyright + Privacy Policy linking to the live `casewhy.com/privacy.html` + Contact mailto). Restyled to the app's own design tokens (surface/border/muted/brand) rather than copying the static page's separate hardcoded palette. The original hero (headline/subhead/email form) was left as-is — this only added the sections that didn't exist here at all. Verified in-browser, full scroll-through, dark mode.
- `src/lib/config.ts` — env var loading/validation; `getUscisFoiaConfig()` added Sep 5 ~8:30am ET for FOIA's separate credential pair
- `.env.local` — sandbox credentials + Neon/Auth vars (gitignored)
- `.npmrc` — `legacy-peer-deps=true`, needed for `@neondatabase/auth`'s Next 16 peer requirement on this Next 15 app
- `eslint.config.mjs` — ESLint 9 flat config (was missing entirely), done Sep 4 late night
- `src/app/api/cron/check-status/route.ts` — status-change polling/diff job, Bearer-secured, done Sep 5 past midnight; refactored Sep 5 evening (CW-37) to call the new shared `src/lib/uscis/check-status.ts` (`checkTrackedCaseNow()`) instead of inlining the fetch/notify/update logic, so `src/app/dashboard/actions.ts`'s new on-demand `checkCaseNow()` can reuse the exact same behavior
- `src/lib/email/postmark.ts` — Postmark HTTP wrapper, no-ops without `POSTMARK_API_TOKEN`, done Sep 5 past midnight
- `src/lib/kb/policy-memos.ts` — CW-31 knowledge base (3 entries) + `findRelevantPolicyContext()`, done Sep 5 ~4:15am ET
- `src/lib/kb/processing-times.ts` — CW-33(a) hand-captured official processing-time figures, done Sep 5 ~4:15am ET; refresh by hand (real browser needed, see note above)
- `src/lib/kb/visa-bulletin.ts` — CW-34 hand-captured Visa Bulletin Final Action Dates, done Sep 5 ~4:15am ET; needs a monthly manual refresh, same reason
- `src/app/processing-times/page.tsx`, `src/app/visa-bulletin/page.tsx` — CW-33/34 pages, done Sep 5 ~4:15am ET
- `src/lib/ai/case-context.ts` — `buildCaseContext()`, shared case-fact/policy-citation prompt-building, extracted from `explain.ts` when `chat.ts` needed the identical thing, done Sep 5 ~6:30am ET
- `src/lib/ai/chat.ts` — `chatAboutCase()`, CW-32's guardrail-tightened chat completion, done Sep 5 ~6:30am ET
- `src/app/api/chat/route.ts` — CW-32's chat API route, re-derives tracked receipt + case status server-side every request, never trusts client-supplied case data, done Sep 5 ~6:30am ET
- `src/app/ask/page.tsx`, `src/app/ask/CaseChat.tsx` — CW-32's real chat UI (replaced the earlier "coming soon" stub), signed-in + tracked-case-gated, done Sep 5 ~6:30am ET
- `src/app/manifest.ts` — PWA manifest (Next App Router convention, auto-served + auto-linked), done Sep 5 ~6:50am ET
- `public/sw.js`, `public/offline.html`, `src/components/ServiceWorkerRegister.tsx` — minimal offline-shell service worker (navigations only, never caches dynamic content) + its registration, done Sep 5 ~6:50am ET

## The fuller record

- **CaseWhy Launch Checklist** (live, checkable, synced via a shared database — this is the running source of truth for open items): https://claude.ai/code/artifact/c67d70aa-7b95-470d-9001-fe1182ffcc18
- **CaseWhy Launch Roadmap** (Gantt, 4 workstreams, 18 tasks, 4 milestones, Sep 7 – Nov 30 2026): https://claude.ai/code/artifact/0ad0e4c6-97d6-467c-ac9f-9ef3103d973c
- **USCIS Torch API onboarding execution spec**: https://claude.ai/code/artifact/8981ad84-0fb3-47a8-bc68-2f484d0db7bb
- **Ideas Claude Project** (claude.ai): `top-3-ai-business-ideas.md`, `top-3-b2c-app-ideas.md`, `immigration-case-companion-mvp-scope.md`

## Immediate next actions (roughly in order)

1. Log sandbox traffic Mon Sep 7 – Thu Sep 10 (days 2-5 of the 5-day requirement)
2. ~~Resolve Google Workspace domain verification~~ — confirmed done Sep 4, 2026 (Verified + Gmail activated)
3. ~~Rename the business contact email to drop "uscis"~~ — turned out unnecessary; the live pages never used the "uscis" placeholder (see note above)
4. File the Florida LLC
5. ~~Build the dashboard UI against `getCaseStatus()`~~ — done Sep 4, 2026
6. ~~`git init`/link this working copy to the existing GitHub repo; set up CI with sandbox creds as Actions secrets~~ — done Sep 4, 2026 (pushed to `nextjs-app` branch, CI passing)
7. Once the 5-day log is complete, email developersupport@uscis.dhs.gov to request the affidavit
8. ~~Send the drafted FOIA product-binding support email~~ — done Sep 4, 2026; awaiting USCIS response
9. ~~Build the plain-language explanation layer~~ — done Sep 4, 2026 evening
10. ~~Report the `nextjs-app` Vercel preview URL~~ — done Sep 4, 2026: it's a production URL, not a preview, see "Right now" above
11. ~~Build persistent case tracking + minimal auth~~ — done Sep 4, 2026 late evening, see Product build above
12. ~~Decide and implement encryption-at-rest for `tracked_cases.receipt_number`~~ — done Sep 4, 2026, same night, see Product build above
13. ~~Build the file upload UI stub~~ — done Sep 4, 2026, late night, see Product build above
14. ~~Fix `npm run lint`~~ — done Sep 4, 2026, late night, see Product build above
15. ~~Build email notifications (SendGrid or Postmark)~~ — infrastructure done Sep 5, 2026 past midnight, see Product build above. Use `info@casewhy.com` for anything email-related, not the user's personal address.
16. ~~Create a dedicated cron-job.org account under `info@casewhy.com`~~ — done Sep 5, 2026 past midnight: account created, "CaseWhy status check" job live (daily 9am ET, Bearer-secured), test-fired successfully (`200 OK`). See Product build above.
17. ~~Create the Postmark account + verify `casewhy.com` as a sending domain~~ — done Sep 5, 2026 ~1:45am ET: domain verified (DKIM + Return-Path), real end-to-end test email sent successfully. See Product build above.
18. Submit Postmark's "Request approval" to leave test mode — needed before real users (any address other than `casewhy.com`) can receive notifications. User-only step, not urgent until there are real tracked users.
19. Optional, not blocking: set up Gmail delegation (`ptakla@gmail.com` as a delegate on `info@casewhy.com`) so Claude Code's existing Gmail tool access can also see/send as the business address when needed for testing.
20. ~~scope and build CW-31, the curated policy/case-law knowledge base~~ — done Sep 5, ~4:15am ET, see "Scope expansion from Peter" above.
21. ~~build CW-33(a), processing-time data~~ — done Sep 5, ~4:15am ET, ships the USCIS-published-ranges version; CW-33(b) comparative analytics correctly still deferred (no accumulated case data yet).
22. ~~build CW-34, visa bulletin tracking~~ — done Sep 5, ~4:15am ET, manual-update approach (both official sources are Cloudflare-blocked from automated fetch, see above) — needs a monthly manual refresh going forward.
23. ~~build CW-32, the "ask a question" AI chat~~ — done Sep 5 ~6:30am ET, see "Scope expansion from Peter" above. One open item: verify the live tracked-case chat flow end-to-end during USCIS sandbox hours (7AM–8PM ET) — not yet done, sandbox was down when this was built.
24. **New, Sep 5 (~4:15am ET):** refresh `src/lib/kb/visa-bulletin.ts` monthly (needs a real browser to load the source — see the Cloudflare note above) — first refresh due early October when the October 2026 bulletin is published.
26. **New, Sep 6 (cloud session):** revisit `src/lib/congress/representatives.ts`'s 434 House entries to verify each member's actual direct DC phone and official website individually (currently all using the Capitol switchboard + the official House lookup page as an honest placeholder — see the note above under CW-39). Not urgent — the current fallback is accurate, just not as direct as the senator entries. Also worth a periodic check on TX-23 (vacant since Tony Gonzales's April 2026 resignation) to add it back once a special election is held.
25. ~~update `src/components/Logo.tsx` to the real mark + hardcoded-green wordmark~~ — done Sep 5 ~5:20am ET, see "Branding decision" above. Also caught and fixed: the brand asset files it depends on were never actually committed (untracked local files only) — committed now, alongside. Left open, not urgent: the two "judgment call" copy changes noted under "Branding decision" (layout metadata + landing hero tagline placement).
