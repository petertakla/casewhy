# New task for Claude Code — round 35: fix the signed-out nav bug, build a shared nationwide state filter

**Status: authorized now.** Three items from Peter, the first two found live on the deployed app (not planning-doc speculation), the third a direct consistency instruction. Fix Part 1 first (it's a real bug on live traffic); Parts 2 and 3 can follow in the same session.

**Part 2's state-selector dropdown is a shared component, not a one-off for accredited representatives.** Peter's direct instruction (Sep 8): apply the same state filter consistently across every directory-shaped entity type in the six-entity "Get Help" system — not just wherever it happened to come up first. Build it once here (generic name, not tied to "accredited representatives") and reuse it in Part 3 (attorneys, this same round) and in round 34 (legal aid orgs, in progress separately — see `round34-legal-aid-orgs-build-and-seed-task.md`, already updated to reuse whichever of these two rounds ships the component first). Every future entity type (DSOs, community orgs) gets it too when built. Employers is the one exception — it's a lead-capture form, not a browsable directory, so there's nothing to filter by state.

**Add a free-text search box alongside the state dropdown, same shared component — not a separate city filter.** Discussed and confirmed with Peter (Sep 8): rather than a rigid city-level dropdown (which would return empty results for most cities once ~100 entries are spread across a whole state), add a plain search input that filters the *currently selected state's* results by substring match against name/organization/city/address. This is a client-side filter over data already loaded for the selected state — no new geocoding, no distance math, no separate "list of cities" to generate from messy PDF-parsed addresses. Default: empty search box, no filter, same as the state dropdown defaulting to all states.

## Part 1 — bug: signed-out visitors see the full app nav, not just "Get Help | Sign in"

Peter reports (Sep 8, testing the live site) that `app.casewhy.com`'s header shows the **full signed-in-style menu** to a signed-out visitor, not the minimal "Get Help | Sign in" pair round 33 was supposed to ship.

**This contradicts round 33's own done-note**, which states: *"Signed-out visitors on `app.casewhy.com`'s landing page (`/`) had no header nav at all... Added a 'Get Help' link next to 'Sign in' in that branch, gated to `!onAppPage` so it doesn't duplicate the nav's own 'Get Help' link on pages that already show the full nav."* Round 33 also flagged its own verification as incomplete — no browser-automation tool was available that session, so this was verified by reading the logic rather than a real click-through, and explicitly asked for a person or a future session with browser tools to confirm visually. This is that confirmation, and it found a real problem.

**Investigate rather than re-guess the fix:**
1. Reproduce directly — load `app.casewhy.com` signed out (private/incognito window or a real signed-out session) and check what actually renders in the header.
2. Look at `AuthHeader.tsx`'s `onAppPage` computation and the two render branches round 33 describes. A likely cause: `onAppPage` (or whatever decides "is this an app route") is evaluating incorrectly for the specific page Peter was on — e.g. it's true when it should be false, so the signed-out visitor falls into the full-nav branch meant for signed-in/app-route users. Confirm which page(s) this happens on (just `/`, or others too) rather than assuming it's isolated to the landing page.
3. Fix so a signed-out visitor sees only "Get Help | Sign in" — never the full `NAV_LINKS` set (Dashboard, Ask a question, CaseWhy Plus, Processing times, Visa bulletin, News, Settings) — on any page where they're not signed in.
4. This time, verify with an actual browser (Claude in Chrome or equivalent) if available this session — a real signed-out click-through, not just a logic read — since that's exactly the gap that let this ship broken last time. If no browser tool is available again, say so explicitly and ask for a human check the same way round 33 did, rather than silently repeating the same lighter-touch verification a second time.

## Part 2 — accredited representatives: expand from Florida-only to all states, build the shared state-selector dropdown

`/accredited-representatives` currently only has Florida entries (98 records, seeded in round 32 from DOJ EOIR's roster, Florida-filtered by design as the "go local first" starting point). Peter wants full national coverage now, with the user picking their state — and wants the dropdown built as a shared piece other entity-type pages reuse (see the note at the top of this doc).

1. **Re-run the seed against the same source, without the Florida filter.** Source is the same DOJ EOIR roster already in use (`https://www.justice.gov/eoir/page/file/942301/download`), same parsing pipeline as round 32 (`scripts/seed-accredited-representatives.ts` — extend it, don't fork a new script), same discipline: real PDF-parsing code, not a summarized fetch. Parse and include **every U.S. state plus DC** (and Puerto Rico/territories if the roster includes them — check rather than assume), not just Florida. Keep the existing skip-lapsed-entries and source-citation logic as-is; this is a scope change on geography, not a rework of the parsing/vetting logic itself.
2. **Add a `state` column to `accredited_representative_directory`** if one doesn't already exist in a usable form (the address field may already contain it as text — check whether it needs to be parsed out into its own column for filtering, since a dropdown needs a clean value to match against, not a substring search on a full address).
3. **Build the state-selector as a shared, reusable component/hook** (e.g. `<StateFilter>` or similar — generic naming, not "accredited rep state filter") and use it on `/accredited-representatives` — standard US state list (+ DC), filters the listed entries to the selected state. Default to showing **all states** with no filter applied (not defaulting to Florida) — Peter didn't ask for a Florida-first default here, just full coverage with the user able to narrow it down. This is the component Part 3 (attorneys) reuses below, and that round 34 (legal aid orgs) will reuse too.
3a. **Add the free-text search box next to the dropdown, as one combined component** (e.g. `<StateFilter>` renders both the state `<select>` and a text `<input>`). Filters the entries within whatever state is currently selected (or across all states, if no state is selected) by a case-insensitive substring match against name, organization, city, and address. This is the "how granular" answer Peter and the cloud session landed on: state is the primary filter (guaranteed non-empty results), free text is how someone narrows to their actual city/area without a rigid city dropdown that would return empty results for most towns given how few entries exist per state right now.
4. **Keep individual permalinks working** (`/accredited-representatives/[slug]`) — check that slugs stay unique now that the same organization name pattern could repeat across states (e.g. two orgs with a similar name in different states); if the current slug scheme could collide, make it state-aware or id-based rather than assuming name-based slugs stay unique at 50-state scale.
5. **Cross-check the parsed national count** the same way round 32 cross-checked Florida — spot-check a handful of non-Florida entries against the source document directly, not just trust a bigger parse to have worked because the smaller one did.
6. Update the page copy/count references that currently imply Florida-only coverage (if any) to reflect national coverage.

## Part 3 — attorneys: wire in the same shared state filter, even though the directory is empty

`/attorneys` (round 27/28) has no seeded entries yet — it's self-enroll only, real vetted attorneys land there as they apply. For consistency, Peter wants the same state filter in place now rather than retrofitted later once entries exist:

1. Add a `state` column to the attorney directory table if one doesn't already exist in a clean, filterable form.
2. Reuse the exact shared component/hook built in Part 2 (state dropdown + text search) on `/attorneys` — same default (all states, no filter, empty search), same UI. Don't build a second implementation.
3. Since the directory is empty, there's nothing to visually verify beyond the dropdown rendering correctly and not erroring against a zero-row directory (confirm the empty-state fallback, e.g. the AILA-directory link, still shows correctly regardless of which state is selected).
4. When an attorney's self-enroll application (`/attorneys/join`) is reviewed and added to the live directory going forward, its `state` should be captured at that point — check whether the application form/table already collects enough address detail to derive it, and flag to Peter if not (this task doesn't need to touch the join form itself, just confirm the data will be there when needed).

## Verify live

Part 1: confirmed on a real signed-out browser session — header shows only "Get Help | Sign in," on every page type, not just `/`. Part 2: confirmed the state dropdown actually filters, the text search actually narrows results by city/name/address within a selected state, a sample of non-Florida entries render correctly with working permalinks, and the total seeded count is sane against the source document. Part 3: confirmed the same combined dropdown+search renders correctly on `/attorneys` against its empty directory. `tsc`/lint clean, production build succeeds, all three parts deployed and confirmed live. Report back and fold into `CLOUD_CLAUDE.md`'s standing status per the usual handoff pattern.
